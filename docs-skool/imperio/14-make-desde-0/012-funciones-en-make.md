# ➕Funciones en Make

> Ruta: Make Desde 0 › ➕Funciones en Make

**🎬 Vídeo (10.2 min):** https://www.loom.com/share/78d32947288d4c93a24da7d263e1531f?sid=31342398-68d6-4888-9a6d-819a2c251fd8

---

En este video, exploramos el uso de funciones en Make para manipular información de manera efectiva.

A través de ejemplos prácticos, aprenderemos a formatear fechas, horas y textos de manera personalizada.

Además, veremos cómo combinar variables y funciones para automatizar tareas y mejorar la eficiencia en la gestión de datos.

## 🎙️ Transcripción

Vamos a ir a un punto un poco más interesante, vamos a usar el mismo escenario que usamos para el módulo anterior. Pero ahora vamos a hablar bien de las funciones, que es algo súper importante para poder manipular información dentro del módulo de Make. Acá vimos que yo usé, yo podía usar solamente la variable. de now, pero me la tira en un formato, vamos a verlo rápido, así se puede ver mejor, si yo pongo la variable sola lo que va a pasar es que, yo corro este escenario, me lo tira en un formato que por ahí funciona por ahí no, aparte acá aparece de esta forma pero después realmente se formatea de otra forma y yo lo quiero en un formato más numérico, quiero día barra mes barra año, nada más. Entonces ahí es donde empiezan a entrar al juego las funciones. ¿Por qué? Porque las funciones son, sirven para manipular variables y crear una nueva variable, lo mismo combinar variables con funciones y poder hacer una variable que sirva mucho más. Vamos a un ejemplo práctico que esto más que hablando se explica y se ve con ejemplos. Yo acá tengo esta variable now, fecha actual 14 de noviembre de 2024. Bueno vamos a ponerle, ¿qué pasa si yo quiero que me lo formatee de otra forma? Voy a ir acá y acá tengo justamente las funciones y voy a ver en Format Date. ¿Esto qué hace? Agarra una fecha y le cambia envía el formato, al formato que yo le pido, entonces esto lo movemos acá, esto si alguno uso Excel es muy similar a Excel, entonces ahí yo le pongo format date, acá tiene que ir la fecha, que es justamente lo que pide ¿no? es ahí en ese, en ese espacio, bueno no lo puedo ver bien, pero en ese espacio marca también de las variables que va entre cada punto y coma o antes de cada punto y coma. Esta particularmente yo la usé muchísimo y lo uso un montón, es de las cosas más útiles que tiene Make acá y yo acá le pongo la fecha y después le digo quiero que lo formate es de otras formas, quiero que me lo pongas en formato día, mes, año, ¿no? vamos a probarlo. Entonces ahora lo que devuelve es esto, entonces es la misma fecha pero formateada diferente. Ahora, ¿qué pasa si yo le quiero agregar que me diga, por ejemplo, Por ejemplo, ¿qué día es? Yo le cambio acá el formato Y esto, me escribe jueves Y así, de esta forma Todo el tema de formatos específicos se lee en la documentación de Make Hay muchísimos Se puede poner con puntos, se puede poner con horas, sin hora Y algo que también está muy bueno Siempre que hablando y quiero basamos un poco en la en la hora en sí no pero es el tema de poder agregarle fechas agregarle horas agregarle segundos agregarle días entonces si yo quiero en vez de poner la fecha actual que quiero poner fecha 10 días más lo que vamos a hacer es ir acá directamente y agregarle donde dice donde dice updates le ponemos 10 esto que va a hacer va a agarrar la fecha de hoy y le va a sumar 10 días Ahí estamos, ahora se pueden combinar variables, se pueden combinar funciones, ya llevo un poco más de complejidad pero es importante que entiendan cómo funciona porque ven que acá combinando un par de cosas ya tenemos la fecha de 10 días más de la fecha actual entonces a mí me pasa mucho cuando yo combino manejo calendarios o manejo equipos de venta que necesitamos automatizaciones y yo quiero que las agendas de los leads se empiecen a lograr desde el punto actual de la agenda, desde que se pidió la agenda, cuatro horas después. No quiero que el vendedor reciba una agenda de en 10 minutos tenés que saltar. una llamada. Entonces ahí empezamos a limitar y yo empiezo a poner una función que está acá, que es en vez de agregar días, es agregar horas y le pongo agregar horas cuatro. Entonces de esa forma yo puedo modificar y puedo actualizar constantemente sin tener que decirle no bueno esto hoy es 14 de 11 entonces ahora le tengo que poner 24 de 11 al 20 de 24 porque esto va a quedar fijo esto no es variable esto es un texto simplemente es una fecha entonces yo necesito usar variables acá y no solamente las variables que están acá sino usar bien todas estas variables y funciones es el tema principal de este módulo, todas las variables y funciones acá para poder manipular esta información. Con un ejemplo más práctico también y más orientado a lo que veíamos acá ¿no? a lo de mandar un mail y demás. y nosotros vemos que muchas veces en las bases de datos acá está ordenado pero hay muchas veces que uno te pone Pablo, otro te pone Oscar, otro te pone, por ejemplo, vamos a ver porque tuvo un error, no importa, pero vemos que acá está estar en diferentes formatos y nosotros lo queremos en un formato prolijo porque vamos a mandar un mail necesitamos que esté con la primera mayúscula y con las, el resto minúscula entonces esto también lo podemos hacer y formatear con make, vamos a hacerlo acá vamos a poner una variable que sea nombre actualizado y le vamos a poner, vamos a tenemos que seleccionar dos cosas, en un principio que seleccionar el nombre que queremos formatear y luego ahora para modificar esto lo que tenemos que hacer para poder arreglar este error que vimos en excel, tenemos que agarrar el nombre como les comentaba recién y después una función que se llama string Esto lo que hace es una función muy simple, que no hay que determinar de nada, solamente ver el texto que se quiere, y directamente te lo transforma a este valor que nosotros queremos, que es la primera letra mayúscula y el resto minúscula. Vamos a hacer una prueba, vamos a ir acá, correr, y nosotros ponemos franco, en mayúscula, me devuelve franco directamente, si yo agarro y le pongo también. De nuevo, lo mismo, la función esa de texto lo único que hace es cambiar y actualizar el nombre a un formato, a este formato que nosotros queremos que es el que casi siempre se usa. Entonces, ¿qué pasa? Si nosotros agarramos esto, nosotros tenemos la variable nombre actualizado, conseguimos el nombre, nos aseguramos que el nombre sea lo que queremos, al fin y al cabo, y luego acá y luego acá nos tenemos que asegurar que en nombre cliente no llamemos a la función o a la variable cliente No llamemos a la variable que estaba en el Google Sheets, sino que llamemos a esta otra, que es nombre actualizado. Entonces ahí ya nos aseguramos que esto esté funcionando bien. Ahora, ¿qué pasa? ¿Cuál es la otra posibilidad? Nosotros no sirve tenerlo guardado en una variable si lo vamos a usar en más muchos módulos sino la manera una vez más simple es directamente esto que hicimos acá, no necesitamos que esté en un módulo de set variable para que funcione, yo puedo poner esto en el mail y en vez de poner hola y seleccionar una variable acá o seleccionar esta, pego acá Y va a funcionar exactamente igual. Está bien, ¿por qué? Por último y ya con esto cerramos. ¿Por qué va a funcionar exactamente igual? Porque nosotros acá le estamos poniendo la función. Entonces, antes de enviar el mail, procesa todas las funciones. funciones y dice bueno a ver nosotros necesitamos este valor, que este valor se puede calcular acá o se puede calcular en un módulo de set variable y demás pero la idea de esto es poder tener las dos opciones usar una función y dejar el resultado para usarlo en muchos módulos después o usar la función dentro de las settings de un módulo, dentro del subject, dentro del content, dentro de lo que sea para usarlo una vez que es también más práctico. Entonces, esas son cosas que es importante que puedan saber.
