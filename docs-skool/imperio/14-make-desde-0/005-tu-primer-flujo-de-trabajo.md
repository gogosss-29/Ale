# 🔧Tu Primer Flujo de Trabajo

> Ruta: Make Desde 0 › 🔧Tu Primer Flujo de Trabajo

**🎬 Vídeo (8.2 min):** https://www.loom.com/share/95e4c1c75ef84ea4a6c8599041fb99e4?sid=3389d1d1-30f2-4b35-b8d5-05b419303bbb

---

En este video, te guiaremos a través de la creación de un flujo de trabajo automatizado utilizando Google Drive y Gmail.   
  
Aprenderemos a detectar nuevos archivos en una carpeta específica de Drive, descargarlos, y enviarlos por correo electrónico.   
  
Veremos cómo configurar cada paso y cómo utilizar variables para que el proceso funcione de manera eficiente.   
  
Al final, comprobaremos la ejecución del flujo enviando un archivo de prueba por correo.

## 🎙️ Transcripción

Bienvenidos al tercer módulo, ahora lo que vamos a hacer es un flujo de trabajo, sí, donde cada vez que haya un nuevo archivo en la, uhm, en una carpeta específica, que yo voy a poner esta carpeta de Imperio Digital, cada vez que haya un nuevo archivo en esa carpeta de Drive, se lo manden a alguien o se envíe por Gmail. Vamos a elegir, creamos un nuevo escenario y acá vamos a Google Drive, escogemos esta, Watch Files in a Folder, que es este módulo específico. Vamos a elegir la conexión, que la conexión hay que hacerla, tiene la guía por otro lado, ya que puede ser un poquito complicado. compleja dependiendo de qué tipo de email tengan, tipo de cuenta y demás. Vamos a elegir acá, tenemos la conexión, ahora elegimos esto yo voy a elegir acá esta carpeta, imperio digital, todo. Esto lo que hace es ve cuáles son los nuevos los nuevos archivos en Emidrive en esta carpeta y no está discriminado de qué tipo de archivo vamos a ver si puede ser cualquier tipo de archivo, lo limitamos a que tenga dos, yo voy a limitar a uno y ahí cada vez que esto lo que hace es dispararse o detectar cada vez que un nuevo archivo está en esa carpeta que seleccioné, ahora vamos a probarlo ahora no subió nada, no hay nada, ahora qué pasa si nosotros subimos algo acá, yo voy a subir un archivo voy a poner un archivo de ejemplo, una plantilla una plantilla de make es esto ya se subió y ahora si lo tiramos de nuevo da exactamente el resultado de éste que es blueprint60shader que es el nombre del archivo que leí. ¿Eso qué significa? Que esto detecta. Ahora, ¿qué pasa si lo tiramos de nuevo? No detecta nada, porque no hay nada nuevo en relación a la anterior ejecución. ¿Sí? Esto significa busca nuevos archivos y una vez que lo busca, ya lo limpia y lo tiene como guardado de que hice, ya lo registro. Entonces, Ahora, ¿qué pasa? Vamos a hacer que cada vez que haya un nuevo archivo lo mande por mail. Vamos a poner otro módulo, sobre archivo email, enviar un email, yo tengo mi conexión acá, voy a poner otro email mío Archivo adjunto de prueba, por ejemplo, bien, el asunto del mail acaba, ¿a quién se lo vamos a enviar?, a que acabamos de abajo, el asunto del email y acá va el contenido del email. Lo importante, lo que queremos y nos interesa acá es añadir un archivo y acá vemos si podemos directamente mapearlo y ven que acá no nos deja nada. elegir directamente el archivo. No es, no podemos simplemente agarrar de un módulo y subirlo a email. ¿Qué es lo que tenemos que hacer en Google, en Google Web Drive? Lo que tenemos que hacer primero es descargar el archivo. Ahora les voy a mostrar cómo. Esto lo vamos a dejar espaciado, vamos a crear otro módulo acá donde vamos a descargar el archivo. y acá ya todo es mucho más fácil, ¿por qué? porque vamos a agarrar directamente esto y acá si vamos a agarrar lo que pasó anteriormente que es esto, esto después vamos a tener otro un módulo que explique bien cómo funciona todo esto. Estas son las variables, básicamente es lo que conseguimos del módulo anterior o de los módulos módulos anteriores y que podemos usar en un módulo futuro. Vamos a hacer un ejemplo más fácil. Acá agarra los archivos. Esto es un ejemplo de lo que devuelve. Me devuelve el ID, el identificador del archivo y acá me lo pide para descargarlo. Entonces yo simplemente aprieto y no me me da un valor específico sino que me da una variable, este este valor va a cambiar constantemente cada vez que haya un nuevo archivo, entonces yo necesito ponerlo así, no tengo que copiar esto y ponerlo acá, sino que la variable para que funcione en cada ejecución que nosotros hagamos, entonces hacemos esto apretamos ok y ahora vamos acá a ver si nos deja vamos ahí muchas veces hay que correrlo, ahora tiro solo y acá ya nos aparece directamente el módulo, esto es lo más fácil que existe, ¿por qué? porque acá el archivo que vamos a tener es el que el que nos aparece del google driver del módulo anterior. Entonces acá no tenemos que completar esto, no hay que completar esto, nada, sino que simplemente aplicamos attachments, vamos a hacerlo de nuevo, attachments y ahí ya se configura solo. Entonces, ¿qué va a pasar acá? Vamos a ver esto. Una manera rápida de entender los módulos y entender los escenarios es apretando el más y vamos a explicar el ritmo. Lo que hace es revisar la carpeta, descarga el archivo y lo manda por mail. Eso es muy simplemente que es lo que hace. Vamos acá de nuevo, vemos que no haya nada malo. mal que muchas veces aparecen estos errores, estas burbujas, pero no hay nada mal y ahora vamos a ver ahí no pasa nada, ¿por qué? porque no hay archivos, entonces como no hay una operación no pasa absolutamente nada, ¿qué pasa cuando yo subo un archivo nuevo? vamos a probar de nuevo acá, subir archivo a esta carpeta y vamos a subir esto, citascanbamec, que es un, bueno lo vimos en los módulos anteriores, tenemos este archivo citascanbamec, yo voy a abrir mi gmail para ver si llega algo, vamos acá y vamos a correr el escenario y ahí vemos que detectó este archivo citascam.make, lo descargó vemos que todo esto es la ejecución de que lo descarga, descargó citas karma make y demás y ahora supuestamente mandó el gmail vamos a ver si llegó vamos acá puede ser un gmail justamente que tarde un poco pero vemos que acá directamente ya me llegó el Gmail. Llegó de Franco, ejemplo ejemplo, archivado junto módulo 3, archivado junto y ahí está. Entonces, ¿qué pasa? ¿en qué podemos usar esto? Esto es básicamente para poder entender cómo funciona un flujo. Nosotros armamos un escenario que luego hace diferentes cosas y va tomando, de información de un módulo anterior para poder usarlo en un futuro. Lo importante de acá es usar bien las variables y usar bien todo esto que fuimos explicando para que justamente funcione cada vez que lo querramos usar. Ahora, ¿qué pasa? Así como está este módulo, así como está esto, no va a funcionar solo. Primero porque va a que está apagado. Vamos a prenderlo pero vamos a ver la frecuencia que nos pide. Esto podemos hacer que revise una vez cada 15 minutos permanentemente, podemos hacer que revise por ejemplo todos los días a las 14 horas y esto que va a ser va a revisar todos los todos los días a las 14 horas, qué archivos nuevos estuvieron en esa carpeta. Entonces, si un día hay 5 o 6 archivos, va a mandar 5 o 6 emails. Si un día no hay ninguno, no va a mandar ninguno. Y así es como funciona en estos módulos específicamente.
