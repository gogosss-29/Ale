# 🧩Conceptos Básicos + Cómo Importar

> Ruta: Make Desde 0 › 🧩Conceptos Básicos + Cómo Importar

**🎬 Vídeo (9.8 min):** https://www.loom.com/share/7f7decce7d4d4786a604474cca67eef5?sid=02b6fecc-e74c-4532-acc1-44dc29464b3a

---

En este video, exploraremos cómo funciona un escenario en [Make.com](http://Make.com), desde la edición de automatizaciones hasta la conexión con Google Sheets.   
  
Aprenderemos a crear y conectar cuentas, identificar errores y trabajar en la programación de acciones automatizadas. Además, veremos cómo añadir filas en Google Sheets y actualizar información de manera sencilla.   
  
No olviden guardar constantemente sus escenarios y estar atentos a las configuraciones!  
  
Nota: si tienen problemas conectando alguna aplicación de Google, estamos trabajando en un módulo específico para solucionar el problema de las autorizaciones 🥳

## 🎙️ Transcripción

En este módulo vamos a ya empezar a entrar en contacto con la plataforma, con los escenarios y demás que es un poco lo que yo les contaba en el módulo anterior pero vamos a ir ya a algo mucho más práctico viendo bien cómo es que funciona específicamente un escenario y qué es un escenario en make.com me hice chiquito acá y ahora vamos a ir acá donde dice crear un nuevo escenario probablemente les salgan ustedes muchos avisos y demás si quieren pueden leerlos siempre está bueno tenerles a tener algo ahí más de guía pero bueno vamos a ir a algo simple sobre esto esto es un panel de control de flujo, el escenario en sí, como le quieran decidir. Esto es un lugar donde nosotros vamos a editar las automatizaciones que vamos a hacer y tiene diferentes opciones como tenemos acá primero para editar el nombre, escenario de prueba, imperio digital 2 y acá estas configuraciones es importante que se les preste atención, ¿por qué? porque vamos a tener primero el ritmo que esto es, me dice que no puedo programar un escenario vacío pero esto es cada cuatro tanto va a correr nuestra automatización o qué ritmo, qué reglas de tiempo va a seguir nuestra automatización. Esto es para guardar el escenario, que es súper importante, no se olviden, guarden el escenario constantemente. Esto es algo ya un poco más complejo, esto es algo un poco más complejo, pero nada, lo vamos a prestar atención a eso. en los próximos módulos a esta parte sobre todo a esto que ayuda a esta parte que ayuda con los errores, luego para dejar notas si trabajan con gente, ver versiones anteriores, autoalineación que ahora lo vamos a explorar y todo esto que también lo vamos a explorar en un minutito cuando tengamos algo más activo. Vamos a ver algo, un caso práctico, yo quiero hacer algo que se fije o que añada una fila, esto es add a row, que añada una fila a mi, a un google sheets que yo tenga. Acá uno va directamente, lo primero que hace, ustedes no van a tener todas estas configuraciones, van a tener una página vacía que les va a pedir que añadan una conexión, por eso es que van a ir acá, directamente donde dice add y les va a salir esto, importar. Es importante ponerle nombre y conexión para poder diferenciarla porque puede haber errores con esto. Es importante que se familiaricen con los errores, es normal que haya errores, es normal que cueste o que sea un poco frustrante también a veces porque esto es nuevo también y hay cosas que andan perfectas. fácil y cosas que no tanto. Pero lo que tiene justamente la plataforma de Make es que te dice muy fácil dónde están los errores y también que tenemos la comunidad donde pueden tirar un mensaje, che tengo un error acá, me pasa esto y lo vamos solucionando entre todos. Ahora una vez que aparece esto le dan a sign in with Google, que yo no lo voy a hacer porque ya lo tengo hecho, siguen los pasos muy sencillos. muy derecho y ya van a ver algo así, entonces yo acá creo un módulo que lo que quiero que pase es que añada una que añada una una fila por ejemplo, vamos a ir a seleccionar a todos a seleccionar acá un archivo que acabo de crear para poder hacer estas pruebas acá elegimos el nombre de de la hoja, en este caso la configuración, esto es configuración del módulo que después vamos a ir viendo, pero vamos a ver que me aparece directamente los valores que puede tener o las columnas de esta hoja de Google Sheets que vemos acá, entonces vamos a ver se agregó un mensaje vamos a poner hola en la columna A entonces qué pasa cuando está conectado, cuando yo tengo conectado mi cuenta de Google Sheets con Make y yo creo acá un módulo que dice ahora añadir una fila, justamente le damos a Run Once que es ejecutar, todo salió bien y vamos acá que aparece este mensaje que dice hola, entonces esto es súper básico, es más que nada para que se entienda el funcionamiento pero para que entiendan que justamente de lo que yo quiero que pase en google en make con un módulo podemos hacer que pase uno le da después ritmo yo por ejemplo podría poner que esta es la configuración de recién que si yo quiero que todos los días a las a las 14 horas se agrega una fila que diga se agrega una fila que diga hola yo lo explico acá en la programación y todos los días a las 14 horas se va a agregar una fila en ese sheets que dice hola ahora qué pasa cuando yo voy quiero avanzar un poquito en complejidad. Acá ponemos otro módulo, podemos ir conectando una cosa con otra, entonces yo lo que voy a hacer ahora es por ejemplo, vamos a actualizar una fila donde va a estar en exactamente la misma misma, en exactamente la misma hoja que yo vi acá, pero vamos a hacer otra cosa. Acá directamente vamos a tener lo mismo pero yo ahora lo que quiero hacer es empezar a tocar en la columna B donde yo vamos a poner acá, acá seleccionamos directamente lo que es la fila, que acá vamos a poner la fila que se usó anteriormente. que ahora lo vamos a tratar a esto. Esto puede parecer confuso pero es bastante simple y vamos a buscar la fila donde se actualizó esto y le vamos a poner la columna B como estas y ahora Visto con esto, vamos a darle a correr y acá lo que vamos a ver es que tenemos una segunda fila que dice hola y otra en cómo estás. Esto se podría hacer de manera diferente pero lo que quiero que vean es que una vez que se aprieta un escenario pasa algo y de lo que pasa puede pasar vamos a tomar información para ponerlo en otro algo, en otro módulo. Estos son módulos, esto es un escenario en sí, este es el módulo, es el módulo principal o el trigger y este es el otro módulo que es, son módulos. ahora vamos a pasar a una segunda parte que es el poder importar las planillas de Imperial Digital a Mac. Vamos a la School, vamos a ir a Classroom, acá. Automatizaciones. Vamos a elegir una. Vamos acá abajo y acá, donde vemos el final de la plantilla vamos a apretar el acá, descargar. Una vez que tenemos esto. vamos a ir directamente al nuevo escenario de make, vamos a crear un escenario nuevo y acá es donde vamos a ver otra de las opciones que es import blueprint, acá seleccionamos el archivo vamos a descargas donde lo tengamos, acá lo tenemos, guardar y ahí ya aparece mágicamente esto, el sistema y ahí ya eso sería un poco todo después es cuestión de configurar configurar y ver que eso lo hagamos en otros módulos y demás, pero así es como se importa una plantilla desde la School de Imperio Digital a make.com
