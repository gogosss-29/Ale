# 🔄Procesamiento de Listas / Iterator

> Ruta: Make Desde 0 › 🔄Procesamiento de Listas / Iterator

**🎬 Vídeo (7.3 min):** https://www.loom.com/share/7e516f1ff9464fbeb743392c47e51853?sid=0b7a74e1-28e6-4d8c-a0a3-0cf5b956db84

---

En este video, exploramos el concepto de iterator en Make, un módulo fundamental pero a menudo mal comprendido. A través de ejemplos prácticos, aprenderemos cómo funciona el iterator, su importancia al procesar bundles y arrays, y cómo configurarlo para optimizar nuestros escenarios. ¡Acompáñame en este viaje de descubrimiento!

## 🎙️ Transcripción

Bueno, vamos a entender un poco ahora qué es el iterator, que es un módulo que se ve bastante en Make, pero que no se entiende mucho para qué funciona o cómo funciona. Las primeras veces que se setea un iterator generalmente da errores, entonces es importante poder verlo bien. Para eso primero tenemos que ver qué es un bundle. que se escribe bundle y que es un array, que se escribe array directamente. Ahora, para entenderlo, para ver bien qué es cada cosa, vamos a usar algunos módulos acá, esto es lo que tenemos ya prearmado, pero vamos a ir acá y vamos a ver qué pasa si yo busco una fila. Voy a setear este módulo para que busque filas en general y esto cuando lo probamos ves que tira acá bundles. Bundle 1, Bundle 2, Bundle 1, 2, 3, 4, 5 y así. En estos casos son paquetes de información. Son filas en sí, que no es necesario iterar, porque ya vienen separadas. Ahora, ¿qué pasa si en otro módulo, en otra situación, como puede ser ésta, que estoy viendo buscar los eventos de un calendario, viene todo junto? tenemos un solo bundle y vamos a ver acá y acá esto que viene acá que está adentro de un bundle y dice array, esto es un array que tiene muchos valores acá pero es adentro, dentro del mismo bundle, ahí es donde hay que usar el iterator ¿Por qué? porque lo que hace es ir una por una dentro de estos valores dentro de este array una por una y ejecutar el resto del escenario yendo así justamente porque si no si no lo aplicamos vamos a ver qué pasa que solamente lo ejecuta una sola vez porque hay un solo bundle, por defecto Lo más importante es esto, por defecto make va a ejecutar el escenario, o sea lo va a correr por bundles, si hay tres bundles lo va a correr tres veces, si hay 10 lo va a correr 10 veces, si hay uno lo va a correr una vez, ahora si adentro del bundle hay un array, a make no le importa. Por más de que estemos seleccionando el array y le pidamos por favor, no hay chance. Entonces, ahí hay que aplicar el iterator. Que lo que hace es agarrar ese array, y profundizar, y ver, y tomar el array con muchos módulos, con muchos, Como si fueran diferentes bundles. Y a partir de ahí, si un array tiene 10 valores, va a ejecutar el escenario 10 veces. Vamos a ir con un ejemplo que es mucho más fácil, ¿no? Vamos a sacar el array de acá, perdón, el iterator de acá y vamos a hacerlo de manera directa. Nosotros lo que vamos a buscar acá son eventos que haya en un calendario y los vamos a poner en un Excel, que yo lo que quiero hacer acá, es ir ahí, le pongo fecha de inicio, acá va la fecha de fin, vamos a ok y vamos a correrlo, vamos a correrlo a ver qué pasa. Ahí ven lo que tira es un Excel. uno y uno. Vamos acá, se ve bien ahí, más allá del formato, que eso es otra cosa, se escribe una sola fila, por más de que haya muchos eventos y de que vamos acá y claramente detectó muchos eventos. pero qué es lo que hace? agarrar el primero porque solamente entiende que hay un bundle, entonces en estos casos lo que se hace es poner el iterador acá en el medio, seleccionamos start end y vamos a ir un poco acá a cómo se configura el iterador el iterador se configura, lo único que hay que hacer es ver y darle las instrucciones de dónde tiene que revisar, dónde hay muchos valores que tiene que revisar, entonces vamos a ir acá y no hay que poner como si estuviéramos poniendo una variable normal que es el start o el end, sino que hay que seleccionar Ahí donde muy chiquitito dice Array, donde dice Array que tiene estos corchetes ahí al costado ese es el valor que hay que seleccionar, que es este. Si yo selecciono acá abajo no va a funcionar, si yo selecciono acá abajo tampoco va a funcionar. Hay que agarrar acá, entonces a partir de ahí ya está seleccionado, acá le agarramos las variables que tiene ahí que esto ya lo busco hace un rato entonces las tengo y vamos a ver qué pasa cuando lo ponemos ahí ahí ven que empieza a ejecutar un montón de escenarios, ejecutó, yo lo paré pero ejecutó 29, que podría haber ejecutado más, porque son, ¿*******? Acá sacó 71. Entonces acá, ¿qué es lo que hace? Agarrar ese array, que antes estaba en un solo bundle, y dividirlo en muchos bundles diferentes. Entonces el resto del escenario se ejecuta como si ese array fuera en diferentes bundles. Y a partir de ahí, ya vemos acá en el Excel, que se crearon un montón de eventos, más allá del formato que no está formateado, está en un formato raro que es ISO, pero más allá de eso, ven que hay un montón de eventos diferentes que se crearon. Entonces ahí, de manera sintetizada y ya para terminar, tenemos tres cosas que hay que aprender acá. ¿Qué es un bundle? ¿Qué es un array? ¿Y qué es un iterador? ¿Un bundle? Es un grupo de valores, que es el resultado inicial que siempre tira media. Es un conjunto de valores por el que se pasa todo el escenario. Yo tengo una fila o diez filas, son diez bundles, que van a pasar todo el escenario cada fila. Adentro de un bundle puedo tener un Array, que es un conjunto de valores más chiquito. digamos, entre comillas, un conjunto de valores adentro del bundle y que si yo no pongo un iterador el escenario no va a procesar todos los valores de acá, sino que va a procesar uno por bundle. Ahora, ¿qué es un array? ¿qué es un iterador? Lo último, un iterador hace que tratemos los array como si fuéramos bundles, como si fueran bundles, entonces profundiza sobre un array y empieza a ejecutar todo el escenario valor por valor. Eso espero que haya quedado lo más claro posible aunque es de las cosas más complejas de entender, dentro de mente.
