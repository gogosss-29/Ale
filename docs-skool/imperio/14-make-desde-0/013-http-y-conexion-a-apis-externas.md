# 🌐HTTP y Conexión a APIs Externas

> Ruta: Make Desde 0 › 🌐HTTP y Conexión a APIs Externas

**🎬 Vídeo (10.3 min):** https://www.loom.com/share/b40cf7f00fed42dfbc120a3c18d055a1?sid=bec7c51c-f2b4-43b7-96d4-404ea44673fa

---

En este video, exploramos el módulo HTTP en Make, abordando cómo funcionan las aplicaciones y las APIs, y la importancia de comprender su funcionamiento.

Mostramos un escenario real de un cliente donde se utiliza HTTP para gestionar membresías y se detalla cómo desactivar membresías, además de la importancia de los endpoints y los métodos POST y GET.

Instruimos sobre la necesidad de leer la documentación API y realizamos una prueba de funcionamiento.

Finalmente, destacamos la variabilidad entre APIs y la importancia de seguir las instrucciones para utilizar HTTP correctamente.

## 🎙️ Transcripción

Ya estamos entrando en un terreno un poquito más, eh, para mí más interesante, para, para algunos un poco más difícil, eh, la realidad es que es un poco más complejo. Por allá, ahora, el módulo de hoy lo que vamos a tratar es un módulo HTTP en, HTTP en Make. Que son estos que nosotros vemos aquí. y configuramos acá. Se ve mucho pero no siempre se entiende cómo funcionan o qué son directamente, para bajar un archivo de una web, para imprimir una página, ¿para qué es? Esto antes de arrancar a meternos en el módulo y el desarrollo en sí Es importante que se comprenda cómo funcionan las aplicaciones y las APIs. Las aplicaciones tienen una interfaz, un protocolo, una manera de llamarlas para que hagas acciones dentro de la plataforma sin tener que entrar. Eso es una API. una, eso es básicamente una API. No quiero entrar en algo súper complejo. Ahora, ¿cómo entramos o qué forma tienen? Tienen justamente formato, muchas veces de HTTP, de una dirección. Entonces, yo puedo acceder a una aplicación externa sin tener que que entrar en esa aplicación usándolo usando una llamada HTTP o una llamada por una API HTTP en español vamos a verlo un poco más más sencillo si yo puedo agarrar por ejemplo acá cómo funciona tenemos un URL acá está el HTTP específicamente que yo digo bueno esto es lo que me da la plataforma que me dice para acceder tenés que enviar un llamado a este url ahora después tenemos diferentes funciones yo en este precisamente esto es una plataforma que ya vi que un cliente necesitaba algunas cosas en hecho tengo dos cosas que puedo hacer por API, por HTTP activar a un usuario, que se hace de esta forma o desactivarlo, que es darle de baja al usuario cada, página, cada aplicación, cada lo que sea. Tiene su protocolo y tiene su manera de usar una API. Entonces hay que leer la documentación API para buscar cómo se usa. Yo me ayudo muchas veces, ahora ya me acostumbré, pero en un principio me ayudaba mucho de ChatGPT para entender la documentación de API y cómo funciona a cada cosa. Todo este contexto, ahora ya sí nos podemos meter un poco más acá y yo iba a armar un escenario desde cero, pero me pareció mejor poder mostrarles un escenario que actualmente funciona, con un caso real de un cliente que me pidió estas cosas y que, un recurso importante para hacerlo, el único recurso era el HTTP. ¿Por qué? Porque si yo busco acá, callabi, me da, me dirige a esto que es una integración de la comunidad, que yo tengo que que hay que comprarla, pagarla y realmente no tiene mucho sentido siendo que yo sé configurarla de otra manera entonces muchas veces cuando no encuentren una aplicación acá tengan que recurrir a un HTTP que es lo mismo al fin y al cabo Entonces, volviendo acá, ¿qué pasa? ¿qué es lo que hace este módulo específicamente? Le saca la membresía a alguien que se le venció el pago. Entonces, este escenario en sí, justamente lo que hace es buscar en un Excel los vencidos, busca algunos. que estén en cierta situación y les da la baja en Kajabi. Luego manda un email notificando todo. Pero esto es donde nos vamos a centrar hoy. ¿Por qué? Porque yo poniendo una url, que es la url de Kajabi, que Kajabi me dice, haciendo, dándole una información, puedo sacarle la membresía al cliente. sin tener que entrar. Y bueno, ¿cómo hacemos esto? Nosotros tenemos un url, acá tenemos dos partes, está el url, que esto es permanente para cada cuenta, y luego está lo que se llama el endpoint, que se escribe así. el endpoint. Esto a ustedes les va a servir para buscar la información que tengan que buscar. Si ustedes buscan endpoints, buscan qué es lo que se puede hacer específicamente sobre una sobre una url o con un http, un endpoint. En este caso es desactivar. Ahora, ¿cómo se activa este endpoint? Mediante un método que se llama método POST. Acá hay muchísimos métodos realmente. Los dos que más importan son POST y GET. Con POST lo que hacemos es darle información. Con GET es recibir información. información entonces vamos a usar método post para esto porque porque yo le tengo que dar el email entonces acá lo que pasa es que nosotros ya vemos bien cuál es la información que necesita para darle baja que necesita el email bien dónde está estaba en una variable antes, entonces yo se lo pongo en una variable acá, que esta variable es mail, listo. Necesito el nombre también, bueno, el nombre lo mismo. La llave, esto es, esto funciona de la misma manera que las variables de anterior, que hablamos hace dos módulos, que es llave y valor, key y value. Entonces, ¿cómo se llama? llama la variable? Name. ¿Cuál es el valor de la variable? Pedro, Oscar, Carlos, Franco y esto es directamente la variable acá. Ahora yo se la tengo que dar en el nombre que la plataforma me lo pide. Esto lleva un poco de complejidad porque no está uniformado sino que que dependiendo de cada cosa y cada aplicación pide cosas diferentes y pide que le des la información de una forma diferente entonces por eso se tornó bastante interesante. Luego justamente me pedía que le dé un external user id que decía en la documentación tiene que ser igual que el mail. Listo, buenísimo. Entonces ahí yo le estoy dando toda la información de los que c********* con las condiciones para que se les dé de baja anteriormente y ya el resto se encarga esto, ahí. Entonces ya no hay mucha más configuración que ésta, en este caso específico, hay veces que queda un poco más hay veces que te piden que pongas un header que no nos vamos a meter en eso pero de esta forma es como nosotros podemos conectarnos con una aplicación externa y aprender a seguir las reglas de esa aplicación externa que se ve en una documentación API. vamos a probar por último vamos a testear voy a yo tengo la membresía de esto de tanto testing me termine dando la membresía pero vamos a sacar vamos a sacarme la membresía, vamos a ir acá, vamos a poner mi email y vamos a sacar la membresía a ver que pasa bueno justamente me parece no encontrado eso de que yo me la saqué pero vamos a entonces vamos a darle la vuelta y vamos a sacarlo y me voy a dar la membresía de esto, ven que acá lo único que hicimos fue borrar el end point directamente en vez de poner deactivate ponemos activate y acá lo mismo entonces ahí, listo, me dio ya el acceso, entonces ahora me lo puedo sacar Ahora quiero que sepan y quiero marcar que esto no significa que sea así con todas las APIs, realmente todo lo contrario, esto va variando de API en API entonces es importante que puedan leer la documentación y verlo. y que puedan, se quedó, se quedó trabado. Esto va variando de API en API. No es que siempre de Activate va a ser algo y Activate va a ser algo. Sino que es importante leer las documentaciones y las instrucciones para usar un HTTP. No intenten mandarse solos porque no, van a encontrar la forma sin saber cómo esa aplicación quiere que le hables, digamos. Vamos a ir ahora con un, quiero mostrarles un poco, otra llamada, que esto es para otro flujo, pero que tiene una aplicación. particularidad. Esto lo que hace es llamar a un bot, avisarle que tiene que que ese bot tiene que hacer una llamada por teléfono a una persona, que haga una llamada y demás, no importa. Lo que importa acá es que en este módulo HTTP ven que tenemos la url y demás y luego tenemos que yo lo tengo guardar una variable, pero acá me pide la autorización en un header, ¿por qué? porque hay algunas hay algunas aplicaciones que piden la autorización específicamente, porque no podemos no puede conectarse cualquier persona o de cualquier forma, entonces nada, eso va a depender de cada caso también es importante leer la documentación y ver qué les pide que pongan en headers porque esto es una autorización con una API Key que es lo más común que hay. Parece súper complejo pero es simplemente ir a la aplicación, buscar la API Key y pegarla de esta forma o muchas veces lo que hacen es poner de header y la piqué, pero bueno eso también lo mismo y nos repetimos a lo mismo la manera más fácil de accionar un módulo http es leyendo la documentación
