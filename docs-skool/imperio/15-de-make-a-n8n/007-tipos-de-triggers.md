# 🎯 Tipos de Triggers

> Ruta: De Make a n8n › 🎯 Tipos de Triggers

**🎬 Vídeo (7.5 min):** https://www.loom.com/share/0c0fdbf4afe64f599075e0bfd1eb23ae

---

En este video exploramos los distintos tipos de triggers en N8N: el punto de partida de cualquier flujo.

Te mostramos cómo funciona el trigger manual, los eventos por app, los triggers por horario (onSchedule), y el webhook con sus opciones avanzadas de configuración. También vemos cómo usar formularios nativos de N8N y cómo testear agentes de IA desde la misma plataforma sin integraciones externas.

Ideal para entender todas las formas de activar un workflow según tu caso.

## 🎙️ Transcripción

Ahora vamos a ponernos a construir algo en n8n. Si, o empezar a gestionar estos nodos. Primero, vamos a ver algo muy muy importante cuando arrancamos un nuevo escenario, que es el trigger. Bien. Vamos acá. vamos a correr por acá, cuando apretas a first step nos aparece what triggers this workflow que es cuál va a ser el trigger que vamos a poner en el escenario. Vamos a ir uno por uno. Primero tenemos esto que es trigger manually. ¿Qué pasa si yo le pongo trigger manually? Si yo le doy acá es simplemente para que yo lo aprete y que sea manualmente y que sólo se accione cuando yo le doy esto a NHRN. Después tenemos algo muy común que es los eventos de las apps, que esto en make. lo teníamos como dentro de los módulos de app los que tienen el instant así que es esto Luego tenemos esto que es onSchedule, básicamente es setearle un ritmo para darle al escenario que se ejecute una vez por día, que se ejecute una vez cada 20 minutos, que se discute todos los miércoles a las 8 de la tarde lo que quieran ustedes si todo esto es donde se puede ir configurando en el trigger bien ahora qué otra cosa tenemos vamos a seguir por acá tenemos el webhook bien acá tenemos un poquito más de complejidad que en make y está buenísimo ahora tenemos esto no hay que añadimos el webhook tenemos dos urls un url de testeo y un url de producción que como ven la única diferencia es que donde dice barra webhook barra si yo lo pongo en test me dice barra webhook guión test barra después es todo lo mismo Luego que tenemos el método que nosotros queremos usar, esto es por protocolo HTTP, en español. ¿Cómo queremos hacer el llamado? Si cuando hacemos el llamado HTTP le vamos a poner POST o va a ser un GET. La gran mayoría de las veces cuando, envías información, querés que sea un post, pero un get también puede servir, va de si estás buscando algo o si no tenés que enviar un cuerpo. Bien, más allá de eso, casi siempre tratando de ponerlo en post, que funciona espectacular. después la autenticación, que esto no es algo que nosotros que se use en este caso, si en algunos casos específicos se puede usar, y la respuesta del webhook, que esto sí es importante. ¿Por qué? Porque cuando nosotros, ustedes tengan en cuenta que esto ya lo saben que si nosotros tenemos un webhook de un lado, tenemos un llamado HTTP del otro. Tenemos un llamado HTTP que le da al webhook una señal o le envía información y el webhook que recibe. Ahora, ¿cuándo recibe esa información el webhook? Da una respuesta inmediata de un estado. 200 generalmente cuando está todo bien al llamado http por lo cual desde este lado sabemos que se envió todo bien esto es si está en inmediately bien si está en la respuesta inmediata Si nosotros ponemos esto, si, o usamos alguno de estas dos, sería como usando un nodo, un módulo en make de webhook response, donde le enviamos información adrede al HTTP, si, le enviamos algún tipo de información. Esto, tenemos dos formas de configurarlo, esto es cuando el último nodo de la secuencia termina, le manda el output de ese nodo ¿si? que es súper práctico porque nos ahorra poner los HTTPS, ponerlo, perdón, los webworks. response y lo otro es usando un nodo de webhook response directamente que es respond to webhook, bien, esto es algo que es muy experimental también tienen que verlo y probarlo y entender un poquito cómo funciona, más que explicarlo pero bueno está bueno que lo lo podemos hablar más allá de eso después tenemos otras propiedades y demás pero no nos vamos a meter mucho en eso lo importante es que comprendan la configuración del webhook directamente esto también está bueno si necesitan no hay ningún problema pero va Eso con los webhooks, ahora vamos a ver qué otros triggers hay, formularios, podemos generar un formulario si nosotros queremos y pasar un link. no si yo agarro y creo que no está configurado pero más allá de eso o sea podemos agarrar y acá crear todo un formato directamente en n8n para que cuando se llene este formulario, venga directamente y se active el trigger. Esto es súper práctico si tienen algo, por ejemplo, que cargar una un archivo simple, fácil ¿no? nosotros vamos a poder usarlo y va a ser fácil para que la persona lo cargue y ya está y se active directamente el workflow después tenemos todas las configuraciones de título, descripción lo que quiera ¿bien? Esto pueden experimentar también. Luego tenemos un mensaje, que esto, ¿para qué funciona esto? Para poder testear o para poder generar directamente una interfaz de un agente de IA, por ejemplo. ¿Por qué? Porque si nosotros, vamos voy para acá, nosotros queremos testear cómo funciona o cómo responde una gente de IA, vamos a poder testearlo directamente acá, no tenemos que enchufarlo todo para meterlo en Whatsapp directamente para testear cómo funciona o cómo conversa la gente de IA, sino que solamente vamos con esto abrimos chat y le podemos escribir hola cómo estás ahora no está ni promteado ni nada, no va a contestar pero si le damos a enter nos da, ven está todo esto entonces si lo configuramos nos contesta directamente en el chat y podemos escribir experimentar un poquito ahí para testear. Ahora por último vamos acá y tenemos estas otras cosas que son bastante particulares, bastante precisas, por ahí en algún momento podemos hablar bien de esto, de los eventos de las cosas que están en el 8n o mismo en los errores, un workflow de errores, hay muchas cosas que se pueden hacer acá, esto generalmente no terminas usándolo, lo usas muy poquito, pero bueno ya tenemos un panorama general de cómo funcionan los triggers y cuáles son los que están disponibles en el Neo4j.
