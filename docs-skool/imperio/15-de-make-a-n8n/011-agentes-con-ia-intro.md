# 🧠 Agentes con IA (Intro)

> Ruta: De Make a n8n › 🧠 Agentes con IA (Intro)

**🎬 Vídeo (4.9 min):** https://www.loom.com/share/041860beabf444dd825d7b0e34c1a39c

---

En este video damos el primer paso para crear agentes de inteligencia artificial en N8N.

Exploramos el nodo de *AI Agent*, las opciones de modelo (como OpenAI), cómo configurar memoria, herramientas (tools) y cómo conectar fuentes como Airtable o flujos externos.

También vemos cómo usar el *Chat Trigger Node* para testear sin integraciones externas y cómo empezar a escribir un buen *System Message*.

Ideal para entender cómo darle inteligencia real a tus automatizaciones.

## 🎙️ Transcripción

Llegamos al punto donde probablemente más se estén esperando, que es el de hacer agentes de inteligencia artificial en Nioh 8N. Acá vamos a detallar mucho y vamos a entrar muchísimo en profundidad, porque es algo que realmente requiere un entendimiento. y requiere comprender qué es lo que está pasando para poder tener desarrollos eficientes, desarrollos que realmente funcionen y no simplemente enchufar algo y pretender que funcione porque sí. Bien, así que vamos a ir súper súper al detalle con esto. con eso, vamos a arrancar directamente con el flujo, bien, para testear acá lo que vamos a hacer y para todo el desarrollo en sí lo que vamos a hacer es usar este nodo que es el nodo de chat. y acá vamos a crear un agent, vamos a apretar AI, AI agent y acá tenemos el menú. Bien, ¿qué sucede con esto? Nosotros tenemos varias configuraciones, en general vamos a a repasar un poquito las configuraciones para para que tengan una noción un poquito por encima antes de ir bien al detalle sobre cada cosa así que vamos a ir primero con el chat model. ¿Qué sucede con esto? Vamos a mover por acá. Acá tenemos diferentes modelos de guía, ¿qué puede pasar? podemos usar para hacer este agente de inteligencia artificial. Nosotros, la realidad es que la mayoría de las veces usamos OpenAI, vamos a ir con OpenAI en este caso, podemos elegir otro, no hay ningún problema y acá elegimos cuál va a ser el modelo que querramos, ¿bien? tenemos que tener las credenciales para que nos aparezca y después a partir de acá si yo elijo por ejemplo O3mini me aparecen las diferentes propiedades y cosas que podemos avanzar y que podemos detallar sobre el modelo de día que elijamos Luego tenemos la memoria, la memoria tenemos podemos hacer un montón de cosas, acá depende mucho de cómo tengan el setup armado de las cuentas y de la complejidad técnica que quieran darle. La realidad es que con esta, la mayoría de las veces va. Así que vamos vamos a dejar esto por acá y acá tenemos algunas settings que luego vamos a también ver un poco y vamos a detallar sobre la memoria, esto es para que tenga contexto y que entienda con quién está hablando el agente de inteligencia artificial y luego tenemos todas las tools que nosotros tenemos diferentes formas de darle tools a la gente de inteligencia artificial. Tenemos acá el Jamaron Workflow, que esto sería lo más similar a las tools que tenemos con escenarios en Make. El Jamaron Workflow, y tenemos un Workflow aparte, pero específicamente para esto. bien eso es a lo que vamos a usar luego tenemos nodos ya un poquito más prearmados como puede ser un HTTP request que esto es un HTTP que pueda hacer el agente de IA y luego acá lo podemos detallar y ven que es el nodo de HTTP pero con algunas leves modificaciones que vamos a ver y unos detalles que vamos a ver en unos momentos. Luego otro tipo de tool que tenemos, vamos para acá, yo si quisiera por ejemplo hacer algo en Airtable, escribo Airtable y acá tengo la Airtable Tool que acá es un nodo es como si yo estuviera poniendo un nodo de Airtable que lo modificó, lo enchufó acá con todas las settings que tengo. normalmente, pero si yo lo borro yo no lo puedo poner de esta forma, no va a funcionar porque esto es una tool, bien? y hay unas pequeñas diferencias con eso vamos a ir un poquito con esto en un futuro y luego tenemos lo que es el agente per se. Acá tenemos desde dónde le llega el mensaje que puede ser el chat trigger node o para testear que es espectacular o bien el ponerle acá y le ponemos una variable que venga desde antes que puede ser el mensaje. el mensaje o el input o lo que sea. Acá es donde el mensaje del usuario va a ir cuando estemos desarrollando. Por lo pronto nosotros vamos a dejarlo en ChatTriggerNode para testear y que esté todo bien. Luego tenemos algunas otras propiedades. Lo que más vamos a trabajar también es el System Message. ¿Cómo hacer un Excel? excelente System Message para que le funcione bien y luego tenemos otras cosas que puede ser las Max Iterations y otras partes por acá que no se usa tanto, sinceramente. Luego tenemos otro tipo de settings que es, esto es normal en casi todos los nodos de N8n, pero bueno esto es el panorama general sobre cómo es la interfaz de la gente para poder ahora ir ya a conocer un poquito todo e ir al detalle sobre cada una de estas cosas que muchas que vamos mencionando.
