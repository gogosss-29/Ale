# ⚙️ Repaso Final Agente IA

> Ruta: De Make a n8n › ⚙️ Repaso Final Agente IA

**🎬 Vídeo (13.1 min):** https://www.loom.com/share/319572d817d549df85a644bb76a1e0f8

---

En este video, hacemos un repaso de la plantilla que hemos estado utilizando y cómo pueden ajustarla según sus necesidades. Hablamos sobre la importancia del webhook y cómo evitar bucles infinitos en las respuestas automáticas. También explico cómo gestionar las etiquetas y la acumulación de mensajes en Airtable, así como el proceso de reconocimiento de mensajes y su manejo. Les animo a que tomen esta plantilla como base y sean creativos al adaptarla a sus propios flujos de trabajo. Espero que esta información les sea útil y les ayude a comprender mejor cómo funciona N8n.

## 🎙️ Transcripción

Para finalizar, vamos a meter un breve repaso de la plantilla, de cómo quedó y de cómo la pueden manipular o ajustar para las cosas que ustedes vayan necesitando, ¿sí? Vamos a ir acá y vamos a repasar, porque esto es algo que, como va a estar subido en la school, es importante que, que esté en la esta explicación que ya hemos hablado en los módulos atrás pero ahora vamos a verlo todo yo. Primero tenemos acá el webhook, importante lo que vimos en el módulo anterior de poder vincular con el canal que Vayana ha usado. tanto sea Wapi o sea Evolution por ejemplo o alguna otra API para recibir y enviar mensajes desde Whatsapp. Puede ser de otro, sí puede ser de otro de otro canal, de otro, de un Instagram o de lo que sea, lo pueden hacer ahí mismo. que ver de qué forma lo vinculan o qué software usan para vincularlo. Después tenemos acá que este filtro a nivel lógico justamente lo que dice es que porque que esto está armado directamente porque es WAPI, porque WAPI te tira señales del webhook cada vez vez que el mensaje es entrante y saliente, entonces si el sistema recibe un mensaje va a disparar de nuevo cuando lo devuelva, cuando responda ese mensaje y no queremos que se haga un bucle infinito de que se estén contestando los mensajes y hablando solo la gente. Entonces acá le ponemos esto, de que solamente le va a contestar cuando el mensaje es entrante. Y esto es algo que es de testeo directamente, lo pueden borrar sin ningún problema. Esto es que el mensaje sea de mensaje. mi número de teléfono que es el que estamos usando para probar bien pero cómo hace si quieren directamente esto no tenerlo no tener esta barrera de seguridad lo borran directamente le ponen a Reboot Condition y listo, ahí ya no van a tener ese fiasco filtro. Desde mi punto de vista es súper importante tener ese filtro hasta el último momento porque si es un número que está ya activo, que está ya en producción, va a recibir otros mensajes y lo va a empezar a contestar a todo el mundo, que ya lo hemos visto. Después acá tenemos, una espera, que esto también, esto es por una cuestión más externa, de que por otro lado tenemos una secuencia donde Wapi cada vez que recibe una nueva condición conversación, en este caso le pone una etiqueta que es para que le conteste esto es circunstancial de WAPIT, bien, es algo que tendrían que configurar, que no hay ningún problema si uno lo quiere hacer, la etiqueta email, le pasamos la plantilla sobre la solamente la dejemos acá, ya está realmente, ya está en la school, en la comunidad, pero vamos a poder referenciarla. Después, justamente después de que esto espera, para asegurarnos que la etiqueta se puso. Vamos a buscar etiquetas. Esto acá se hace un GET directamente, que esto lo que hace es buscar las etiquetas usando WAPI, que ve las etiquetas del chat directamente. Luego acá lo que hacemos es juntar las etiquetas, que esto es directamente una expresión en JSON, que esto es para juntar lo que sale directamente desde acá, bien, todo lo que sale, todos los arreditos, todos los límites los objetos lo junta en algo donde lo podemos evaluar directamente que es acá le ponemos el filtro y van a pasar este filtro solamente los que las etiquetas tengan y ya. Esto es por el nombre que nosotros le pusimos a las etiquetas no por el Por ejemplo, si ustedes le ponen otro tipo de nombre a las etiquetas y juegan de otra forma con las etiquetas, también se puede hacer sin ningún problema. Luego acá tenemos el tipo de input que esto lo que hace acá es filtrar para ver según el tipo de mensaje. Tanto sea texto, voz, texto. lo que sea, esto son las, es la evaluación que se hace de si el tipo de mensaje es texto, si el tipo de mensaje es voz y le ponemos el rename output para que sea claro, entonces acá por ejemplo si yo no le pongo rename output me va a aparecer texto 1 imagen link, yo lo que quiero que me aparezca acá para reconocerlo fácil es audio, entonces acá ya aparece bien, por cada una de estas rutas se procesa diferente y esto acá es súper importante que se entienda cómo funciona, que tenemos el, nosotros lo que tenemos que enviarle a la gente es texto. Se lo enviamos, si llegó texto directamente lo unificamos acá, lo unificamos con un input ¿para qué? para después poder hacer el llamado acá, el unificado mucho más fácil, entonces tenemos siempre un bloque de input, input, input, input para poder cambiarle el nombre a todo lo que viene atrás que es otra cosa. Tenemos en texto que es directo, en audio que lo que hace es descargar el audio con un HTTP, con un GET, porque justamente si nosotros vamos a WAPI tenemos esto que se hace un autodownload, entonces acá nos llega directamente, si es audio nos llega el link, si es imagen nos llega el link, eso no hay ningún problema. Entonces vamos acá, de nuevo, y hacemos el get directamente a este url. Después de acá se transcribe el audio que se le manda directamente y esto también lo mismo, está configurado para que sea así, para que lo agarre, agarra la data del módulo anterior y después acá texto y va acá, con la imagen, con la imagen le pasamos directamente el link porque le ponemos el input type image url y acá lo tenemos de esta forma tenemos que poner este modelo porque es el modelo que mejor interpreta imágenes así que vamos bien, después acá tenemos el url y ya estamos en input imagen le ponemos esto y acá viene algo que es un poco particular de whatsapp que le ponemos nosotros lo que va a recibir recibir, lo que va a recibir el agente es una imagen, o sea, es la descripción de una imagen. Entonces acá le ponemos, entre paréntesis, el usuario juntó una imagen, dos puntos, y el contenido de de la imagen y abajo esto si está se le va a poner porque nosotros este es lo que se llama la caption que es el subtítulo que se le pone a la imagen cuando lo mandamos en cuando lo mandamos yo le mando mando una imagen por whatsapp y a esa imagen abajo le mandamos pongo un texto de mira esto que pasó eso es la caption entonces acá es lo que es esto justamente lo que está que si está va a aparecer si no no va a aparecer no nos va a dar problema y después el link que justamente acá guapi te sepa para el link del texto pero realmente nosotros lo que hacemos es esto, es pasarlo directamente acá y listo, después de ahí ya sigue su camino bien y acá tenemos esto vamos a repasar acá que es input limpio, nosotros acá renombramos input limpio a todo lo que sea, lo que venga del nodo anterior, que es JSON y que diga INPUT, que tenemos todo esto. Después de acá, tenemos un buscar al usuario, porque acá tenemos una tabla donde nosotros registramos al usuario. que se le pone de esta forma. Bien, nosotros ahora vamos a repasar un poquito lo que es Artable. Vamos a repasar muy brevemente qué es lo que tienen que tener en Artable, en una tabla, en lo que sea, esto ya lo guste a ustedes. ,que es una tabla de acumulación de mensajes, ¿sí? ¿Esto por qué? Porque justamente nosotros acá lo que tenemos y lo que empieza acá, ,es la recolección de los mensajes fragmentados. ¿Esto qué significa? Esto que ven acá, es la capacidad que tiene la gente de esperar un minuto para tener todos los mensajes del usuario. Esta es la A. lógica, bien, no nos vamos a meter mucho en el table porque realmente se va a tornar muy confuso, pero si tienen que comprender que acá lo que hace es buscar la usuario en una tabla, que si existe ya vamos a ir acá y tenemos dos rutas, por decir así. La primera ruta es que si existe y ya se le hace directamente y está esperando el mensaje, vamos a poner el subir el primer mensaje input en progreso. Esto qué significa? Esto ya lo habíamos explicado en los primeros módulos pero vamos a repasarlo un poco. Busca el usuario, tenemos dos rutas, si el usuario, vamos a ir por abajo primero, si el usuario no existe en la tabla va a crear un registro del usuario y va a esperar un minuto. Este registro del usuario que tiene, tiene el número de teléfono, que se usa como el ID, y la acumulación de mensajes. esta acumulación de mensajes es el mensaje directamente y abajo se le cambia el estado y se le pone en progreso ¿por qué? porque después si yo mando otro mensaje en esta ventana de 60 segundos yo le mando otro mensaje y acaba ir por la otra ruta va a ir cuando busca el usuario me va a encontrar porque en la ejecución anterior 30 segundos antes se creó entonces busca el usuario existe true y ve y dice está esperando esto es si el status si el lo que cargamos acá es esperando o no. Si no es esperando, que sería realmente que la gente está esperando, si no es esperando va a acumular el mensaje porque significa que está en progreso y significa que estamos dentro de la ventana de 60 segundos. y lo acumula acá, y ven que acá queda la acumulación de esta forma, se le actualiza y se acumulan todos los mensajes y si está esperando va a ir por esta ruta, o sea si es el primer mensaje va a ir por esta ruta No el primer mensaje de toda la conversación, sino el primer mensaje después de que la gente conteste. Va por acá, sube el primer input y lo que le da en progreso. Que esto, este, es igual a este. Lo único que acá crea el record y acá lo baja. actualiza. Luego van a esa ventana y todo lo que pase desde en esas ventanas de 60 segundos va a ir para acá, va a ir a este a este nodo donde va a acumular en el mismo espacio toda la información. Luego acá, lo que hacemos es traer la acumulación completa y una vez que se trajo la acumulación completa ya se establecen esperando de nuevo para que el resto de los mensajes pase a la otra tanda. Esto lo vamos a borrar porque esto está extra Y ahora tenemos reconocimiento de etiquetas que hace el mismo reconocimiento de etiquetas que antes. ¿Por qué? Porque en el medio pasó un minuto, un minuto y medio tal vez, y hay veces que el cliente lo que hace es sacarle el mando a la IA poniendo alguna etiqueta. ¿Por qué? Porque también como tenemos el reconocimiento de etiquetas, acá, la persona, el cliente que está sosteniendo el Whatsapp Business, tiene la posibilidad de sacarle el mando, sacándole la etiqueta. Entonces acá hacemos la misma cosa. evaluación, el reconocimiento de etiquetas y solamente si reconoce la etiqueta pasa al agente, el agente ya lo repasamos muchísimo no nos vamos a meter en eso y luego acá tenemos el enviar la respuesta del usuario, si que esto es con super simple. Ese es un pequeño repaso de cómo está funcionando y cómo funciona parte por parte el agente de día, este flujo específico, con la idea de que ustedes puedan realmente comprender cómo funciona y cómo puedan gestionar los de ustedes, que puedan armar los de ustedes, que puedan tomar este como base si se quiere y a partir de ahí ir acomodando, ir agregando, ser creativo y poder realmente manipularlo como quieran ustedes, más allá de quedarse simplemente con una plantilla. Así que Así que nada, así ya terminamos los módulos de N8n, espero que les haya servido, espero que les sea de utilidad para aprender y para ponerse muy muy creativos porque N8n es una plataforma muy muy potente, compleja como vieron lenta a veces, pero es muy muy potente. Así que les mando un saludo y muchísimo éxito.
