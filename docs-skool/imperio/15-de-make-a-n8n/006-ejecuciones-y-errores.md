# 📊 Ejecuciones y Errores

> Ruta: De Make a n8n › 📊 Ejecuciones y Errores

**🎬 Vídeo (8.6 min):** https://www.loom.com/share/db4f39755fa34155ae5f9b56877d10dd

---

En este video exploramos cómo funcionan las ejecuciones en N8N: el historial de todo lo que pasó en tus workflows.

Vemos cómo leer los logs, identificar errores, entender los outputs nodo por nodo, y cómo usar filtros para revisar ejecuciones fallidas. También te mostramos cómo hacer debugging, reintentar flujos corregidos y por qué N8N da más errores que Make (y por qué eso no es tan malo como parece).

Clave para aprender a fallar bien y resolver más rápido.

## 🎙️ Transcripción

Bueno, ahora vamos a ver algo un poquito más, eh, detallado, digamos, que son las ejecuciones. Como vemos acá, tenemos dos pantallas, Editor y Ejecuciones. Las ejecuciones son los logs, sí, que se conocen. Y vamos a ver cómo funcionan. todo lo que pasó, no, nos aparece toda esta información, bien, está perfecto que es todas las veces que se ejecutó el workflow con horas, minutos, segundos y c***** tiempo le tomó le tomó. ¿Por qué nos importa ver c***** tiempo le tomó? Porque a partir de ahí vamos a poder ver cuáles son las ejecuciones más fuertes, ¿no? A mí no me interesa ver esta ejecución que son 22 milisegundos porque es una ejecución que no prosperó. Sí, es un mensaje que llegó al número, pero que no es no pasó nada, porque no c****** con el primer filtro como ven acá, ahora si yo voy acá puedo ver, vamos a ver que esto tomo 6 segundos, vamos a ver bien que es lo que sucedió. Esto fue la ejecución del testeo de lo anterior. Vamos a volver, vamos a ir para acá abajo a ver otra ejecución, alguna ejecución un poquito más compleja. 40 segundos. Bien, esto fue de ayer a las 5 de la tarde. y vamos a ver, me parece que vamos a poder ver una ejecución completa acá, bien vemos acá esto, si? entra, pasa por todo el flujo, vemos que acá ya aparece todo lo que fue pasando, bien y nos aparece el resultado también de todo esto si seguimos acá y quiero ver por ejemplo el resultado de esto y vemos que nos aparece de una manera súper clara input, nodo, output que es toda la información anterior, vemos acá la configuración del nodo y luego el output del nodo, este output es lo que antes ustedes veían en la burbujita del que decía 1 arriba del nodo, del módulo del nodo. de make, bueno el output lo vemos acá, si? y está parseado, que podemos verlo en formato de tabla, podemos verlo en este mismo formato de acá, muchas veces en tabla es súper cómodo sino en JSON también, si? Eso ya va por gustos. Ahora, siguiendo un poco por acá, vemos que era un input de texto, pasó, pasó, pasó, no hubo ningún problema mayor, luego acá tenemos el agente de IA que vamos a profundizar luego en los elementos. gente desde IA, bastante, vamos a profundizar ahí, pero lo importante acá que vean es que esto te marca en verde los, las tools que usó, las herramientas que usó, c****** veces también, entonces eso queda súper bien. claro y está buenísimo y vemos que acá envió la respuesta c****** sin ningún problema. Ahora, ¿qué pasa con esto? ¿qué pasa cuando hay errores? Nosotros tenemos acá una herramienta que es este filtro de las ejecuciones ¿Dónde podemos buscar por error, error, cancel, queue, running y demás? Esto es un error, fue cancelada manualmente, quedó en espera, está corriendo o un éxito o esperando. ¿Qué vamos a buscar? Vamos a buscar las del error, también otra cosa antes de meternos acá, acá también podemos ver y filtrar por el horario de ejecución, por etiquetas y demás un montón de cosas que podemos hacer mismo por el ID del ejecución. ejecución, que cada ejecución tiene un IDE. Como ven acá, esto que aparece acá al final, esto es la IDE de la ejecución. Pero bueno, por ahora nos vamos a centrar en buscar las que tienen errores para mostrarles esto. Vamos acá a esta ejecución anterior, que esta fue una ejecución unos siete minutos antes. de la que vimos exitosa y vamos a buscar directamente qué es lo que pasó nosotros vamos acá y vemos que el rojo está acá que era la consulta en erte bien qué pasó acá nosotros vemos que nos da el error y que nos dice último mensaje del lead expect the date time but we got esto sí entonces vamos acá y vemos que acá está el error ¿por qué? porque el table porque el servicio que estamos usando espera de un dato de una forma específica. Esto que pasa, nosotros vamos a ver muchos más errores acá en el 8n que en make porque make te protege un poco de los errores. Acá no, acá te deja poner lo que quieras y bueno realmente muchas veces pasa que hay errores esto justamente es un error dentro de todo normal que es un error de formatos porque esperan que nosotros porque Airtable espera recibir la información en una táctica en un formato de fecha puro, cuando nosotros lo tenemos ahí formateado entonces ahí tenemos que ir y acomodar, ahora qué pasa? esto les pasó ahora puede pasar que tengan algún error de teclado tipos de datos, que por ejemplo tenés un campo en Airtable o en algún otro servicio que espere un número y le des un texto, ahí te va a dar un error, porque justamente pasa eso, no importa si el texto es sí o pero una, si está interpretado o formateado como texto te va a dar error, entonces te puede dar error entonces nada, depende de varias cosas, paciencia, porque es muy probable que tengan muchos más errores acá que en lo que tienen en make si es importante leer todo esto para saber cómo resolverlo, ¿si? Entonces, ¿cómo justamente? ¿Cómo podemos hacer para resolverlo? Acá tenemos varias formas, ¿bien? Primero muchas veces acá esto fue una ejecución que ya fue resolvida, ¿no? suelta, pero muchas veces acá lo que sucede es que te dan el retry, no te da el, vamos yendo a esta otra ejecución, que es una ejecución más corta que un error por otro lado, no, fue el mismo error, pero corrió rapidísimo. vamos a si le doy a acá me aparecen dos opciones podemos reintentar con el workflow original desde el nodo con errores que es básicamente darle como para que siga sin ninguna modificación mayor agarrar y reintentarlo con el nodo que tenemos hoy en día modificado. ¿Qué pasa? Si nosotros le damos con esto no va a funcionar porque tenemos ya está, ya dio error y si nosotros le damos con él pero si nosotros le damos con esto probablemente funcione porque nosotros ya modificamos y eso resolvimos ese error. Ahora, esta es otra cosa que se puede hacer, vamos a darle acá a ver si me lo agarro bien y bueno justamente como filtramos con error no nos va a aparecer pero vemos acá y está corriendo la ejecución que acabamos de poner que aparece abajo retry of execution 9672, entonces está reintentando y va a ejecutarse bien porque ven ya está procesando, no hay ningún problema. vamos a volver, ahora vamos a esperar primero a que termine, ahí vuelvo y seguimos con esto, bien ahora tenemos que ya fue ejecutada, como ven le tomó un minuto cuarenta, pero siguió directamente de acá y pasó todo sin ningún tipo de problema, bien. Eso es como sucede cuando resolves algo y después le das al retry, lo procesa sin problema. Ahora, otra opción que tenemos, ¿no? yendo a cualquier otra ejecución, nos da esto para meternos en el, nos da este botón que es para meternos en el editor de boolear y de volver a hacer lo mismo que hicimos recién, ¿si? así es como funciona el debugging en n8n de las ejecuciones que es ya fueron fallidas, así que vamos ya con el siguiente módulo
