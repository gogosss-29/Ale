# 🏗️ Caso Real Completo

> Ruta: De Make a n8n › 🏗️ Caso Real Completo

**🎬 Vídeo (9.4 min):** https://www.loom.com/share/fc7cdd624da54a3d83d6c258a41f5675

---

En este video vemos un caso real de agente de IA integrado con WAPI y múltiples tools en N8N.  
Te mostramos cómo funciona todo el flujo completo: desde recibir un mensaje, filtrar, acumular inputs, reconocer etiquetas, hasta pasar por la lógica de control y finalmente activar el agente de IA.

Aprenderás cómo armar flujos que funcionen en producción, con lógica preventiva, manejo de mensajes fragmentados, y respuestas adaptadas según el contexto. Ideal para entender todo lo que pasa **antes y después** del agente para que realmente funcione en escenarios reales.

## 🎙️ Transcripción

Dicho esto, vamos a hacer un repaso ahora sobre todo, ¿bien? Tenemos acá esto. En este caso estamos usando WAPI, que es un servicio ya bastante conocido, ¿sí? ¿Cómo hacemos para conectar WAPI con N8n? Súper simple, con un webhook, ¿bien? Nosotros creamos un damos un webhook, este webhook es de N8N y cuando vamos a WAPI vamos a poder seleccionar y vamos a poder escribir al webhook, vamos a hacerlo muy rápidamente. Entonces en WAPI vamos a entrar directamente a el número de teléfono que tenemos levantado vamos a settings y en settings nos aparecen los webhooks, bien? nosotros podemos agarrar y escribir directamente la url que nos aparece acá, bien? esto ya está hecho así que no va a haber ningún problema. Tenemos esto acá, ya está conectado y lo que vamos a usar WAPI es simplemente como un paréntesis para recibir y enviar, es un canal directamente y nos va a facilitar algunas cosas a lo largo del flujo bien tenemos que recibimos el mensaje una vez que recibimos el mensaje lo filtramos si esto pasa específicamente con wapi porque porque wapi te envía webhooks también cuando el mensaje es enviado, entonces hay que poner estas condiciones. Acá y esto es algo extra porque era lo que tenía yo para testear en su momento, entonces esto es que el número sea igual, que el que envía el mensaje sea igual a quien? a mi número de persona. Vamos a avanzar acá, tenemos un módulo de espera tenemos esto que lo que hace es buscar las etiquetas del agente, voy a buscan las etiquetas del chat, ¿por qué? porque justamente lo que queremos acá es que le hable a las personas que tienen un etiquetado, así que tienen una etiqueta que es de IAIN, bien vamos acá tenemos esto que es lo que hace es juntar las etiquetas, toda esta parte es para reconocer, esto se usa cuando hay doble comando, cuando hay un agente de IA en un número que a veces que se usa y a veces que lo usa un humano, entonces a partir de ahí es que se genera esta lógica, también por eso es que nosotros tenemos este esperar, este wait, porque hay otro flujo que lo que hace es ponerle la etiqueta a los chats nuevos, entonces esperamos a que eso se accione por las dudas y se sigue. Luego acá verifica que según la lógica de etiqueta que nosotros tengamos, no tiene que ser esto, tiene que ser la lógica de etiquetas que nosotros querramos, que nosotros pongamos, que nosotros usemos, que esté de acuerdo y que haya que responder. Acá justamente lo que hacemos con Guapi es ver de qué tipo de mensaje estamos hablando, si es un texto, si es un mensaje de voz, si es una imagen, si es un link que lo manda como link preview, cuando mandamos un link y un mensajito abajo y luego acá lo que ponemos es el output name para diferenciarlo, que le ponemos el tipo directamente. Luego acá unificamos toda la información, si es texto, si es audio, si es imagen o si es link, siempre lo que vamos a hacer es procesarlo primero y luego pasarlo al input. Como ven, la lógica de acá, estamos hablando, ni siquiera vamos a la gente todavía. Primero lo que hacemos es procesar todo el mensaje, armar todo el mensaje, limpiarlo, acomodarlo, ajustarlo, ver si la persona hay que responderle y demás. Y a partir de ahí, ahí vamos a seguir y después mandarle a la persona. Luego tenemos otra función que esto es específico en los momentos donde lo quieran usar. Esto es una recolección de mensajes fragmentados. ¿Esto qué significa? Que nosotros tenemos una ventana de mensajes acá que es de 10 segundos puede ser de 60 segundos donde según esta lógica que nosotros creamos buscamos al usuario en la base de datos si el usuario existe vamos acá no vamos a pasar un poquito buscamos esta fórmula y además que es una base de datos interna que tenemos armada en la agencia. Luego si existe, si JSON-ID exists, va para la ruta de arriba, si no va para la ruta de abajo. Si va para la ruta de arriba vamos a ver el estado de ese, de ese usuario que también lo tenemos de Airtable y como lo tenemos de Airtable ahí vamos a ver si está esperando o no está esperando, si está, perdón esto es al revés en sí, pero si no está esperando, si es el primer mensaje mensaje directamente si nosotros con esta lógica lo que vemos acá vemos que es el primer mensaje del usuario ahí lo que vamos a hacer es guardarlo en una como si fuera una data store, guardarlo en el table y a partir de ahí ir a la ventana de mensajes. ¿Cómo se ve esto? De manera práctica. Yo le mando el primer mensaje a la gente y no me reconoce y demás o mismo es el primer mensaje que va, me guarda el primer mensaje y espera un minuto. ¿Por qué espera un minuto acá? Para que si hay otro mensaje, si yo le mando por ejemplo hola, enter, ¿cómo estás?, enter, el cómo estás, el segundo mensaje no va a ir por la misma ruta y vamos a estar actuando en ese minuto de espera. Yo le mando el segundo mensaje y va a ir acá y lo va a acumular. ¿Esto qué significa? Que va a guardar de esta forma. Va a guardar la acumulación de mensajes. ¿Esto qué significa? acá cuando guarda la acumulación luego en cualquier contexto tanto sea el contexto que le manda un solo mensaje o que le mando muchos mensajes va a funcionar de la misma forma el agente de IA ¿por qué? porque luego tenemos un nodo que es traer la acumulación completa entonces si yo en ese minuto de espera, desde el primer mensaje hasta que pasaron 60 segundos. Yo mandé un solo mensaje o mandé 20, luego acá tenemos un nodo para traer la acumulación completa. Lo mismo pasa si es un nuevo un nuevo usuario, acá lo único que se agrega es que crea el registro pero volvemos a lo mismo y a partir de ahí todas las acumulaciones van a parar acá. Esto es lo interesante de N8n que podemos salir y volver directamente. Y luego acá ya establece directamente en esperando otro mensaje como para que que se vuelvan a acumular otros mensajes. Acá creamos una consulta, esto es crear en otra, en una base de datos paralela, que esto es accesorio, es dependiendo de lo que necesiten ustedes, y como acá pasó un minuto y medio, más o menos un minuto, vamos a volver a hacer el reconocimiento de esto. etiquetas que vimos antes. ¿Por qué? Porque muchas veces el usuario, el operador, en casos de nuestros clientes, en ese minuto y medio tiene la costumbre de sacarle el mando a la IA y si nosotros antes reconocimos que estaba la IA y luego vemos que pasa un minuto y medio por ahí no está, no hay que contestar y hay que frenarlo. Pero si no, sigue su carril sin ningún problema. Por eso volvemos a reconocer las etiquetas, juntar y hacer todo el filtro que tenemos ahí. Ahora, acá llegamos recién después de hacer todo el filtro. eso. Después de hacer todo ese proceso llegamos a la gente de IA. No, como ven hay muchísimo procesado antes para que una gente de IA funcione de manera real, más allá de el tener un pequeño agente o lo que sea, pero para que funcione en la práctica hay mucho procesamiento antes del mensaje y antes de llegar a la gente de IA. Y ahora en la gente de IA vamos a ver un poco todo lo que tiene y cuál es la función. En este caso lo que estamos viendo es que en la gente de IA es un agente inmobiliario que está especializado en ventas y demás no hay mucha mucha ciencia sobre esto no creo que reparemos mucho sobre esto porque ya vimos muchísimo tiempo esto entonces acá tenemos el prompt tiene las tools y demás y luego ahí sí envía la respuesta del usuario, nada más. Luego de la gente ya mandamos la respuesta por wapi, lo mismo que usamos como canal, le enviamos esto, le enviamos el to acá, enviamos el cuerpo del mensaje y estamos, como ven luego tenemos acá todo lo que es el agente con muchas herramientas, muchas muchas cosas en el medio y que vemos que están prompteados de esa forma, vemos acá que tenemos usa esta herramienta para obtener información de la propiedad, acá lo mismo mismo asigna la conversación a un especialista humano y acá tenemos esto mismo la url de un http puede ser variable si eso es lo lo fuerte de n8n que casi lo que se les ocurra lo podemos variabilizar entonces no hay ningún problema. etiqueta, descarta, todo hasta agendar la visita directamente. Acá tenemos la estructura en general para que se comprenda y puedan, de nuevo repito, que puedan comprender y usar esta oferta. formar la suya según las necesidades que tengan comprendiendo cómo funciona esto y ahora vamos a ir directamente al testeo y ver cómo funciona esto en vivo.
