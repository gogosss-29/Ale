# 🧱 Canvas y Flujos

> Ruta: De Make a n8n › 🧱 Canvas y Flujos

**🎬 Vídeo (8.2 min):** https://www.loom.com/share/6a4551adb141454f8de26b27d720f110

---

En este video nos metemos de lleno en el canvas de N8N: el espacio donde se construyen los workflows.

Te mostramos cómo se diferencian los nodos de Make, cómo funcionan los routers, qué libertades (y riesgos) te da esta estructura y cómo referenciar datos entre nodos usando JSON.

También exploramos ejemplos reales de flujos complejos, cómo ejecutar test para acceder a variables previas, y cómo empieza a tomar forma la lógica más avanzada de N8N.

Ideal para entender cómo se piensan y organizan las automatizaciones en esta plataforma.

## 🎙️ Transcripción

Un poquito más profundo ahora, en lo que es el Canvas de Workflows, ¿no? Este espacio que ven acá, que es donde construimos las automatizaciones y los agentes de guía en N8n. ¿Qué sucede con esto acá? Hay varias particularidades que, como ven, es bastante diferente a make que es lo que muchos de ustedes ya conocen, sin embargo vamos a marcar algunas cosas ahora la idea es que puedan tener un panorama general de cómo funciona esto y luego vamos detallando parte por parte sobre cómo las diferencias, sobre las cosas para aprender bien cómo funciona esta plataforma. Primero como ven acá, ven que tenemos un filtro que los filtros en make estaban acá en las líneas, digamos entre nodos, entre módulos, es lo mismo. y acá todos son nodos, todos son bloquecitos, entonces acá tenemos un filtro y otro filtro pero funcionan diferente cada uno, lo vamos a ver después en otro módulo del curso. eso sí hay algo que seguramente todo esto le llama la atención que es esto que es una particularidad muy importante de N8n que al N8n poder manejarse más manual digamos al poder gestionar más la muchas más cosas, también permitir muchos más errores y dar más libertades, podemos salir de en varias rutas como ven acá que esto es similar a un router de Make, si vamos a darle clic acá vemos que es un router, tenemos las diferentes rutas que nosotros pusimos con condiciones y luego van directamente todos al mismo nodo, bien, que esto no se puede hacer en Mate. pero más allá de eso es algo importante. Otra cosa que como ven que se puede hacer, yo estoy acá haciendo esto, salgo y vuelvo acá, le puedo poner, esto lo testé hace unos segundos, puedo volver, si por ejemplo, si yo agarro y saco otro y lo pongo directamente acá, me va a dejar, entonces hay muchas cosas que se pueden hacer y a nivel de libertades, N8n tiene muchísimas libertades. con la posibilidad de tener errores ¿no? Entonces acá hay algo que muchas veces pasa que es como hago yo para en este nodo referenciar o para sí para en este lo que escribo acá referenciar algo. que me sirva tanto sea que viene de acá, de ahí, de input texto, audio, imagen o link en este caso ¿Por qué? porque nosotros estamos acostumbrados a referenciar por el nodo, de decir bueno yo quiero que, me tomes la variable de el nodo de WAPI, por ejemplo, que lo estamos nombrando acá y toda esta estructura y demás que ahora vamos a detallar ahí, pero acá para poder hacerlo no tenemos que referenciar un nodo específico porque cada nodo tiene diferentes nombres y no podemos poner el mismo nombre a los nodos entonces tenemos que referenciar específicamente de esta forma que es el signo de moneda JSON, eso que referencia el nodo anterior, el nodo inmediato anterior, todo lo que venga de acá, de acá, de acá, de acá es JSON para este nodo, ahora para este nodo si yo le escribo JSON va a referenciar lo que viene de éste porque es el nodo inmediato anterior, si yo voy acá JSON text, pero solamente para éste vale, bien? ahora si nosotros ponemos esto, lo ponemos de esta forma siempre va a funcionar porque también nosotros nos preocupamos de hacer un set antes de hacerlo, es decir, similar a un set en make donde le ponemos el nombre de la propiedad que es input, entonces acá nosotros tenemos el nombre del nodo referenciado y de la propiedad bien vamos que es input. Vamos a detallar todo lo que es la sintaxis foot en otro nodo en otro en otro módulo del curso pero bueno vamos a está bueno que conozcan un poquito y que ya de entrada puedan ver cómo funciona esto. Como ven acá los flujos pueden ser bastante más complejos gracias a esto vemos que acá salimos acá se vuelve un punto un punto muerto pero luego seguimos por la misma ruta y acá por abajo nos salteamos directamente a la misma ruta entonces y llegamos acá y se sigue, o sea, hay muchísima complejidad de flujo que se puede dar mismo acá tenemos lo mismo que salimos y volvemos Sin embargo, esto es algo que no, que, nada, o sea, tranquilamente se podría borrar esto, ¿no? No hay ningún problema, pero, nada, vamos a dejarlo así como está. Y después vemos que podemos integrar agentes en el flujo. y gracias a este tipo de estructura nosotros podemos generar un agente de IA por ejemplo, que nos elija para qué ruta va y luego vuelva y entrega el mensaje, o sea son muchas cosas que se pueden hacer con con la flexibilidad de n8n a nivel de flujos, que es todo lo que vimos ahora ahora detallando un poquito más de lo que es el javascript si nosotros vamos a por ejemplo esto a este vemos que acá directamente estamos referenciando a, referenciamos con el nombre del nodo específico y luego empezamos a detallar, esto es importante que tengamos una panorama geográfica para después ir bien, bien, bien en detalle sobre cómo se hace eso, pero para nosotros poder tener que en el input nos aparezca todo lo que viene de antes, tenemos que correr un testeo, sí, y para correr un testeo tenemos ya estamos en un detalle un poquito más más fuerte, si no es tan simple como test sino que nosotros vamos acá y tenemos que agarrar una vez que está piñada esta información, que vemos que nos aparece todo esto y demás, que ya está piñada piñada, ahí nosotros vamos a ir por ejemplo acá y vamos a Execute Previous Nodes y ahí arranca a procesarse todo lo que nosotros necesitamos para hacer que nos aparezca al momento de escribir, si vamos acá, nos aparezca todo esto, y acá nos aparecen las variables para seleccionar y demás, hay veces que bueno ven esta es otra cosa ¿Qué pasa? Nosotros acá tenemos InputEnLimpio que es este nodo, fíjense como nos aparece la sintaxis, como nos aparecen las variables. Tenemos acá que es InputEnLimpio que este nos aparece, pero si yo busco lo que me aparece, lo que me aparece es parecería con input en input audio no me va a aparecer porque no corrió esa ruta, corrió la ruta de input link entonces yo voy a input link y aparece pero en el resto no y después sigo acá me aparece todo bueno transcribir a******* lo mismo la misma lógica y acá tenemos toda la información que recolectó en este testeo y acá podemos llevarla directamente a arrastrar de acá hasta acá, pero bueno es importante poder conocer cómo funciona la, cómo funciona, funciona, la sintaxis que eso lo vamos a ver en detalle después. Bastante información por ahora, vamos ya directamente al siguiente módulo del curso.
