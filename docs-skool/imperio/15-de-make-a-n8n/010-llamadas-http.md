# 🌐 Llamadas HTTP

> Ruta: De Make a n8n › 🌐 Llamadas HTTP

**🎬 Vídeo (4.7 min):** https://www.loom.com/share/2c27bc662cf94c1bb366f6273dd4d916

---

En este video vemos cómo usar el nodo HTTP en N8N para hacer llamados a APIs externas.

Aprenderás a configurar métodos, headers, autenticación y cuerpo del mensaje. Además, exploramos una función muy útil: *Importar desde CURL*, que te permite armar una llamada desde una API Reference y pegarla directo en N8N sin hacerlo manualmente.

Ideal para integraciones más técnicas o personalizadas, sin complicarte con código.

## 🎙️ Transcripción

Ahora vamos a detallar un poquito cómo usar un módulo o un nodo HTTP en el NLHL, bien. Vamos a entrar por acá, vamos a, acá a escribir HTTP. Tenemos el nodo, muy similar. lo que vemos en make, vamos a ver tenemos varias formas de hacer este tipo de cosas, nosotros siempre recomendamos que se busquen las API reference que es esto que se ve acá, bien y acá vamos a tener un montón de información que ya probablemente sepan cómo usar o conozcan un poco cómo hacer un llamado HTTP con la información que está acá en una API Reference. Bien, ahora nosotros vemos acá que tenemos los diferentes métodos, todo lo mismo, la url authentication, que esto generalmente lo dejamos de esta forma y hacemos la autenticación, la ponemos en los headers, que es más fácil también Y luego acá seguimos con lo que es la parte de headers y el body, el cuerpo, bien, que podemos ponerlo con nombre y valor o directamente con un JSON, que esto es como lo hacemos en un montón de ocasiones. Más allá de esto, Eso, esto es extremadamente similar a lo que ya conocen con Make, se iban llenando los campos, vamos a buscar acá, buscamos la última. url, copio url, ya tenemos esto acá, authentication, bueno estamos, headers, ponemos accept. Esto es lo que nos indica acá, bien, ven que esto tenemos header, accept, application.json, url esto, el request es un GET, tenemos un GET, bien. ningún problema y ahí vamos avanzando con todos los campos, que esto de nuevo repito no es nada nuevo, nada complejo y no vale la pena que nos metamos mucho en esto, si hay una funcionalidad espectacular dentro de n8nx que es el import curl, si? vamos a entrar, vamos a hacer un nuevo nodo, http y vamos a ver esto, ¿no? importar curl, ¿qué es el curl? es esto que tenemos acá, si yo voy a por ejemplo sendTextMessage, vamos a ver, y yo empiezo a completar estos parámetros, esto pasa en muchas API Reference, no solamente en WAPI, esto es algo genérico, pero si yo arranco a completar para completar esto y veo a ver qué es lo que, Buenas tardes ya tenemos el cuerpo, todos los requeridos más que nada, lleno de los requeridos acá y algunas otras cosas si se quiere, la URL está, pongo el token, que yo ahora voy a buscar mi token y ponerlo acá de WAPI y acá tenemos todo armado, ¿no? Entonces, ¿cómo se procede con esto? ¿Qué beneficio tiene N8N? Yo una vez que lo armé acá, que armar acá es súper fácil porque ya tenemos todo bien armado, todo perfecto. Vamos a copiar, que es todo lo mismo, pasa en muchas API references, se tienen que asegurar que acá diga curl, copio, voy a esta sección, lo pego, importa y ya salvo. Acá tenemos todo esto, ya se arma solo y se pone todo como nosotros lo armamos en esa pantalla. Lo vamos a probar Y vamos a ver si me llega el mensaje Justo dio un error que es typing time, esto que se marcaba acá Vamos a borrar este parámetro directamente, porque si no, vamos a verlo acá, typing time no está requerido así que vamos a ahorrarlo directamente e intentar nuevamente. vamos acá y ahora si yo voy a mi whatsapp me parece que me llegó el mensaje sin ningún problema
