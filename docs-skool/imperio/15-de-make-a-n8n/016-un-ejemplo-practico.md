# 🚀 Un Ejemplo Práctico

> Ruta: De Make a n8n › 🚀 Un Ejemplo Práctico

**🎬 Vídeo (4.4 min):** https://www.loom.com/share/cb8beeac51e446c0a2f1a41b264d8430

---

En este video vemos un ejemplo real de un agente de IA funcionando con múltiples tools conectadas: desde llamadas HTTP simples hasta workflows más complejos como la transcripción de audios.

Exploramos cómo estructurar el *System Prompt* con variables dinámicas, cómo el agente decide qué tool usar según el contexto, y cómo aprovechar la flexibilidad de N8N para adaptar el comportamiento del agente según datos, horarios o tipo de usuario.

Ideal para pasar de la teoría a la acción y ver cómo todo empieza a integrarse.

## 🎙️ Transcripción

Un caso un poquito más práctico sobre un agente de IA con diferentes tools que esté funcionando. Bien, vamos a verte todo esto. Y acá vemos que tiene memoria, tiene modelo, y tiene, eh, tres tools, ¿sí? Estas tres tools son dos. de llamar a otro workflow, como vemos, y acá tenemos un HTTP. Vemos que estas tres tools están a disposición directamente de la gente y vamos a repasar un poquito el circuito. En este prompt que tiene armado acá. Nosotros vamos acá. Y vemos que, nada, está rol de la gente, objetivo, procedimiento operativo estándar, pasos, taca, taca, taca, todas las rutas y demás. Pero lo que quiero que repasemos también es esto, es que nos tomemos un minuto, porque tampoco es algo extremadamente extenso. para mencionar que acá nosotros le ponemos esto que ya lo habíamos visto antes de darle una variable directamente en el PROM pero esta variable que ahora es now.format esta variable puede ser cualquier variable nosotros podemos hacer que la gente de ahí ella obtenga, por ejemplo, la información de un Airtable para traerla acá y que la tenga, que tenga el nombre del cliente dentro del System Prompt, por ejemplo, o nada, que se quede, que haya una parte mucho más grande, ¿no?, del sistema. del System Prompt como variable. ¿Esto qué significa? Que podemos realmente jugar muchísimo, porque si queremos que tenga un tono diferente por cada persona o para cada persona, si queremos que le hable de una forma, como una a las mujeres, como una forma a los varones, a los hombres, es algo que se puede hacer porque justamente podemos variabilizar dentro del System From. Es algo que es tan simple o tan complejo como lo quieran decir, como lo quieran armar Pero, bueno, ya va a depender un poco de sus casos de uso, de la creatividad que quieran usar en esto. Pero está muy bueno trabajar en la mesa para que lo tengan en cuenta como solución cuando necesitan que la gente se comporte de manera diferente, dependiendo de de el entorno, dependiendo de la hora, dependiendo de variables justamente, podemos meter variables y jugar con esto acá. Así que nada, eso era un buen punto para introducir. Vamos a testear acá y vamos a resetear las cosas. session y ahora vamos a decirle hola que tal y vamos a ver un poquito como funciona tenemos que memoria, uso acá, todo esto, ningún problema y le digo por ejemplo y acá lo que debería hacer es llamar a esta herramienta por cómo está prompteado. Vamos a ver, la llama sin ningún problema y ahora me devuelve el valor del dólar en la cotización del día porque esto es una API que hace específicamente eso. Esto es un caso donde como es un solo llamado, un solo nodo, lo puede hacer directamente con una sola tool. Ahora, en otras partes, en otros nodos, otros casos, como puede ser estos dos que ahora vamos a ver, necesitamos ver otro workflow directamente. Acá se configura directamente como un nodo HTTP, que ven que le ponemos una descripción como lo que vimos antes, se configura normal, pero ya directamente la response del HTTP. http la respuesta se toma como output de la tool y que la puedo usar para responder. Ahora si vamos a otro workflow, a otra tool acá, vamos a ver que este es una tool que nosotros tenemos. hicimos en su momento para transcribir audios, también hay una descripción y demás que era específica para ManyChat y si vamos al workflow vamos a ver que es un workflow un poquito más complejo si se quiere ¿ven? un HTTP, transcribe y ya diríamos que directamente el output de el último el último nodo que es la transcripción lo toma como output de la de la de la tool así que ya con eso está estaría bien si es un pequeño error repaso como para ver algo un poquito más práctico no tan abstracto como lo que veníamos viendo antes para que vean cómo funciona.
