# 🛠️ Integración de Tools

> Ruta: De Make a n8n › 🛠️ Integración de Tools

**🎬 Vídeo (13.3 min):** https://www.loom.com/share/9edbd156ad8a4bcc893d41caa9f30a4e

---

En este video vemos cómo conectar herramientas a tus agentes de IA en N8N, desde nodos simples como Google Calendar hasta flujos completos usando *Code Generated Workflow Tool*. 

Aprenderás a configurar tools por servicio, cuándo conviene usar un nodo directo y cuándo armar un workflow separado, cómo pasarle variables al agente y qué detalles cuidar para que accione bien cada herramienta. 

También te mostramos cómo escribir los prompts para que el agente entienda cuándo y cómo llamar cada tool. Esta parte es clave para que tu IA no solo hable, sino también ejecute. 

Una tool hace un "llamado" a algo en específico, a ejectuar una acción (en términos básicos). Clave para llevar tus agentes de IA del chat a la acción real.

## 🎙️ Transcripción

Ya vimos cómo configurar un System Prompt, cómo configurar un modelo de guía para conectar a la gente de inteligencia artificial y la parte de la memoria. Ahora vamos a repasar un poco más sobre lo que son las Tools, que es realmente donde se viene un poco lo interesante. ¿Por qué? Porque n8n tiene, como ya hemos mencionado, una forma bastante sencilla de configurar los tools, ¿bien? Muchas veces lo que nosotros necesitamos es simplemente un nodo, un módulo muy simple que, por ejemplo, ejemplo, vea la disponibilidad en un calendario. De esa forma, nosotros lo que tenemos que hacer directamente es buscar esto, que sería un Google Calendar Tool. Vamos a tener una tool por cada servicio, por cada API de servicio que tengamos. Esto significa que nosotros hechos un seleccionamos acá y luego acá por ejemplo si yo quiero seleccionar esto luego voy configurando todo, del recurso, si quiero obtener un evento, quiero tener un calendario, si quiero por ejemplo ver la disponibilidad, selecciono acá cuál puede ser el calendario específico y luego acá tengo diferentes opciones, vamos a tener pero justamente si yo quiero en vez de obtener un calendario crear un calendario evento es el mismo nodo, si? porque directamente cambio acá, creo y tenemos lo mismo, si? entonces esto es respecto a los, a las tools que por ahí están un poco prehispánicos terminadas, por decir así. Ahora, ¿qué pasa si queremos hacer algo un poco más complejo? Algo como lo que hacíamos a veces en Make, que era usar un escenario entero de Tool. Justamente acá tenemos una opción muy simple que es Code Generated Workflow Tool, que es esto. y acá directamente nosotros seleccionamos el workflow que queremos asignar, el workflow que queremos crear, que queremos asociar. Generalmente cuando estamos en esta altura lo que tenemos que hacer es ir a la crear un nuevo workflow y acá ya nos tira directamente esta pantalla que el trigger, como ven, es este. Este trigger nos da para poner algunas variables, que si nosotros lo ponemos ponemos por ejemplo nombre, número o teléfono, lo que sea para luego acá hacer, no importa lo que vayamos a hacer acá, pero para obtener estas variables y usarlas en el flujo vamos a acá, recargamos y nos aparece lo que escribimos ahí, bien, y acá es donde empieza la parte un poco más fuerte o más potente de N8n, que es el punto. poder llenar las variables con inteligencia artificial. ¿Esto qué significa? Que nosotros tenemos acá el nombre, ¿no? Vamos a suponer que queremos agarrar y acá mandar un, vamos a hacer un ejemplo un poquito más práctico, ¿no? Vamos a poner email y acá tenemos y acá le ponemos que le va a mandar un mail, vamos a e-mail send message, creo que no tengo credencial todavía y luego acá tenemos conexión, mensaje, send y acá lo ponemos en to, acá lo que vamos a tener es justamente usando las expresiones json.email Acá vamos a tener que, hola, vamos acá, ahora vamos a detallar un poco sobre qué está pasando. json.nombre, yep. mensaje de testeo. Ahí estamos. ¿Qué sucede con esto? De nuevo, volviendo un poco a lo que habíamos aprendido antes, tenemos que el nodo anterior es JSON, lo podemos mencionar directamente como Json y acá tenemos las variables JsonNombre, JsonTeléfono, JsonEmail, lo que sea. ¿Esto qué significa? Que cuando llegue la información de este workflow, de esta tool de N8n, va a, a llegar acá va a activarlo y va a mandar el mail según lo que se envíe bien ahora qué pasa cuál es la parte que nos falta de esto lo que nos falta es hacer que el cargo es hacer que esto lo llene la inteligencia artificial entonces vamos a aprender como configurar una tool para que funcione con inteligencia artificial y para darle buen criterio a la inteligencia artificial. Acá hay mucho de prompting, entonces esto es importantísimo. Cuando no estés llamando una tool, en su caso, en su flujo, en su agencia, lo más probable es que lo puedan arreglar jugando con esto, cuando no esté llamando una tool específica. Entonces vamos a ir de arriba para abajo, esto va a ser una tool que sea, vamos a ponerle nombre, nombre es importante, enviar email. Y vamos acá y vamos a ponerle cuando tengas que confirmar información, envía un email con esta herramienta. Lo que tenemos, no hay ningún problema con esto, vamos acá y ahora es donde vamos a poner que dejar que el modelo define los parámetros, que es en vez de escribir acá en fixed o en expression Lo que vamos a hacer, vamos a apretar esto y acá le ponemos una descripción, nombre, si alguno ya vio Relevance por ejemplo. ejemplo, va a ver que es muy muy similar a la configuración que llevamos ahí. Entonces acá ponemos nombre del cliente y acá vamos a lo mismo, teléfono indicado por el cliente. y acá le podemos poner, por ejemplo, comenzando con 749 porque es el número de Argentina, sin incluir el más, y acá podemos también acomodar un poquito de formato y en mail lo mismo, vamos a ir acá email indicado por el cliente listo ya tenemos esto bien y ahora el agente de IA cada vez que llame a esta herramienta, si nosotros la agarramos y la pronunciamos acá, que ahora vamos a avanzar un poco con eso, va a llamar acá y esto se va a activar. Ahora, ¿qué pasa? Realmente para hacer un solo nodo no tiene sentido. lo que hicimos porque es estar usando una herramienta de llamar a otro workflow cuando es solamente un nodo que podríamos haberlo hecho haciendo esto ¿no? Gmail tool y tenemos la misma información seleccionó la credencial y no aparece todavía seleccionar credencial, todo y a partir de acá, nada, ya le puedo poner en el mail esto, la info y todo lo que hicimos recién en cada variable, entonces no sería el caso ahora. ¿Para qué caso si sirve? Cuando tenemos que, por ejemplo, en dejar registrado esto. Entonces vamos acá por ejemplo y vamos a ver el table vamos a create a record voy a crear una base para avanzar con esto y que se entienda un poquito más el concepto de cuándo hay que usar el record con another workflow como tool, Bien, ya teníamos todo más o menos mapeado, pero ¿qué es lo que queremos demostrar acá? Que el llamar a otro workflow nos sirve cuando tenemos que tener dos o más nodos dentro de una misma herramienta, y si es un solo nodo, vale la pena hacerlo de otra forma, si. Vamos a testear un poquito acá, vamos a a ir acá vamos a configurar un poquito esto para que, para que accione la herramienta y vamos a testear el mensaje Acá, ¿qué acabamos de hacer? Vamos a darle una simple línea para ver cómo funciona, no es algo súper complejo, lo que buscamos acá es que accione la herramienta nada más y acá ven que tenemos que calear la tool, que tenemos que llamar la tool de esta forma. Esto, sea la herramienta que sea, en el prompt hay que tratar de llamarla de esta forma, entre comillas. Y algo importante que se me pasó es que tiene que estar en una string, tiene que estar sin espacios en el medio de la herramienta. Entonces acá, si nosotros vemos, acá me encuentro. confundir, porque tiene un espacio y hay que nombrarlas de esta forma. Vamos acá, guardamos, ahora vamos a testear, digo hola me llamo franco Y ahora vamos a ver, está usando, acá vemos cuando accionamos, vemos que llama al modelo, guarda en memoria y demás. Y acá que me dice, hola Franco, bienvenido a peluquería X17. Para empezar, ¿me podés pasar tu mail y tu teléfono con código? ¿verdad? Así te envío un que Y mi número es, Y ahora vamos a vamos a verificar que esto esté acá activo, si esto, otra cosa importante, cuando tienen este trigger no tenemos que activar nada, queda siempre activo, si, entonces acá ponemos esto y ahora vemos que actúa sin ningún problema vemos que ahí accionó no hubo drama
