# 🔄 Condiciones y Rutas

> Ruta: De Make a n8n › 🔄 Condiciones y Rutas

**🎬 Vídeo (10.3 min):** https://www.loom.com/share/9ae2a843450d489abc5099adbb5ff8c6

---

En este video empezamos a construir un flujo real en N8N usando Airtable como base de datos.

Aprenderás a usar nodos clave como *search records*, *if*, *update* y cómo aplicar lógica condicional para que el flujo se bifurque según los datos. Vemos la diferencia entre *filter*, *if* y *switch*, y cuándo conviene usar cada uno.

Ideal para automatizar decisiones como “si cumple esto, hace A, si no, hace B” y actualizar datos de forma dinámica.

## 🎙️ Transcripción

,en algunas partes esenciales o nodos esenciales de los flujos de N8n. Primero vamos a experimentar con esto. Sí, para que ustedes puedan ver un poquito. Es una tabla de 29 propiedades. que tenemos en un table de testeo con algunas, algunas columnas nada no muy relevantes pero bueno tenemos algo de información para poder jugar un poquito vamos acá primero como estamos realmente testeando vamos a poner de trigger esto, que es iniciar cuando yo le haga clic, bien y después vamos a empezar ya a construir que es vamos a ir a our table, vamos a poner search records por ejemplo, yo tengo acá un montón de credenciales pero vamos a ver esta que es una credencial que tengo para todo lo que está en mi cuenta y tenemos acá, acá vemos que podemos elegir y podemos manipular mucho desde en un mismo nodo que esto es algo que es buenísimo a diferencia de make te da la posibilidad de poder cambiar el método que sin tener que generar todo desde cero si nosotros tenemos por ejemplo si yo quiero hacer un surf y tengo acá la información y todo y lo arranco a configurar y luego lo cambio por un get, si? puedo seguir usándolo en el mismo, es el mismo nodo, si? pero bueno más allá de eso, eso es algo para casos específicos vamos a ver acá, yo tengo esta, pongo recurso, registro, un record, operation, search y elijo acá la base, la tabla y demás, que tenemos acá, qué vamos a hacer, vamos a ver acá y vamos a filtrar, esto es lo lo mismo una fórmula porque acá ya estamos en la configuración de servicio vamos a poner que tipo sea igual a departamento entonces le ponemos tipo igual departamento y vamos a testear directamente, vemos que acá nos da todo esto, ven como por ahí en algunos momentos es mejor obtener como tabla, otros como shift si esquema generalmente es bastante confuso pero la tabla nosotros tenemos acá y tenemos todo lo que obtuvo y lo podemos ver de una manera súper sencilla como si fuera un excel, bien? entonces vamos acá vemos a ver si hay algo más que querramos poner, bueno vamos a configurar algo más unas cosas pero nada, nada raro si, estas son todas las mismas configuraciones que en Make pero lo tenemos de otra manera, lo tenemos en otra, en otra, en otra interfaz. Vemos acá, acá hay algo también importante que es el retry button. fail, podemos poner esto, si nosotros acá en n8n no tenemos lo que es el error handler nativo, si, sino que tenemos que ponerle el retry on fail que es el node específico, la configuración específica que tiene tiene bastantes limitaciones pero después tenemos esto, ¿no? de qué hacemos con el error. Bueno, podemos parar, podemos seguir y ver cómo se puede hacer, o sea hay muchas cosas acá bastante técnicas que después en algún punto lo vamos a ver bien pero está bueno que se vea. para un poquito esto ya que lo estábamos viendo. Nosotros vamos a ponerle de nombre al nodo buscar departamentos, mi name, siempre recomendamos que en la búsqueda nombramiento de los nodos, lo escriban todo en este estilo que es sin usar espacios, este estilo se llama camelback realmente, pero nada de esta manera queda mucho más prolijo y es más fácil que sea todo compatible, porque que hay veces que tenés algún error de compatibilidad, no sabes por dónde es y es porque tenías un espacio ahí en el medio. Así que nada, es un método preventivo. Bueno, ven que acá nosotros tenemos un item y acá 24 items. Esto es lo que en médica se llamaban los bundles. Bien, ahora vamos a empezar. a jugar un poquito con esto. Vamos a agarrar y vamos a meternos en esta parte que es la parte de flow, que es donde podemos nosotros empezar a jugar con todo lo, con el flujo, con los items y demás. Vamos a poner un un if que sería como un router, si? vamos a conectar, ya se conecta automáticamente acá nosotros que tenemos? tenemos una condición o podemos poner un grupo de condiciones y luego tenemos true o false, si? vamos a ver Vamos a ponerle que si el piso no es planta baja, por ejemplo, si no es pv, entonces vamos a ponerle poner, vamos a agarrar de acá directamente piso, seleccionamos la variable porque acabamos de testear, recuerda lo que vimos antes que me pone JSON porque es el nodo anterior punto para seleccionar y piso que es esto igual a y vamos a ponerle not equal to y le ponemos pepe, bien acá tenemos una condición y vamos a ponerlo que sí es, no? si es planta baja o sea que sería el falso vamos a agarrar y vamos a poner por ejemplo vamos a poner una ahora elegimos algún algún nodo que vayamos a poner, vamos a ponerlo por acá update record, por ejemplo y si es true vamos a ponerle también lo mismo pero pero vamos a ponerle otra configuración, vamos a poner acá y acá tenemos que si el departamento que buscamos es planta baja va a ir a esta ruta y si el departamento no es planta baja va a ir a esta respuesta, vamos a crear acá un campo que sea un checkbox que diga aceptable por cliente porque por ejemplo el cliente que el cliente no quiere planta baja entonces vamos a, ahí tenemos dos campos con los que sean planta baja vamos a ponerle vamos a escribir algo acá y los que no sean planta baja vamos a ponerle algo acá aceptable por el cliente. Vamos a ir directamente a esto, vamos a configurar, configuro estos nodos que ya estaban acá, vemos esto directamente vamos a ver qué pasa con esto, vamos a ponerle autorizado aceptable, record, update, sigue lo mismo, lo tenemos otra operación y acá tenemos, vamos a elegir todo lo mismo, propiedades test y acá vamos a lo mismo, ya que tenemos todo esto tenemos que ejecutar los nodos prea los nodos prea para poder tener de manera fácil el input, no? y acá vamos a ver sacamos de buscar departamentos y id using to match esto, si? y acá acá tenemos todo un JSON un poquito más complejo que ya lo vamos a ver, ya vamos a detallar todo esto pero está bien y acá le ponemos en aceptable por cliente true y esto sucede con los booleanos. Ahora qué vamos a hacer, tenemos aquí acá esto vamos a lo que voy a hacer también para que sea más fácil voy a copiar pegar conecto y este lo borro y ahí ya tenemos actualizar y le ponemos no actualizar aceptable, rename, y ahora lo que vamos a hacer es cambiar esto acá y decir es planta baja el cliente no lo quieren por ejemplo y así de simple ponemos un router de toda la vida como ya lo reconocen muchísimo y le damos a test y ahora se arranca a actualizar en dos segundos y algo muy importante que no consumió ninguna operación, esto es un filtro que, perdón, esto es un filtro Sí, es medio un router, funciona para ponerle directamente una condición pero que el resultado sea true o false, hasta dos rutas. Si queremos tener más de dos rutas no nos sirve este nodo, nos sirve este de switch que nosotros tenemos acá y podemos arrancar a generar, tenemos una ruta al routing, routing, routing y acá tenemos cuatro rutas que mismo le podemos poner nombre de, digamos acá renameOutput ponemos testeoruta por ejemplo, entonces acá va a aparecer y en el momento en que llamemos luego a una variable o lo que sea vamos a saber y va a estar mucho más claro que esta es la ruta que queremos. Es lo mismo ya conocen cómo funciona la lógica y demás no vamos a detallar mucho ahí pero así que sepan que para dos rutas o sea para una evasión evaluación que sea true o false va a este nodo para poner muchas rutas va a este nodo y para poner un filtro que antes lo poníamos en en las líneas entre módulos en make que lo poníamos acá tenemos que poner un filter así y esto cuál es la diferencia con esto que éste la condición que le pongamos acá solamente si es true va a seguir en el va a seguir el flujo si no hay dos rutas entonces queda filter para una condición y solamente que pase si es true y filter que es este nodo para tener dos rutas una true y una false y este switch que es el router para poner la cantidad de rutas que quieran.
