# 🤖 Modelos IA en N8N

> Ruta: De Make a n8n › 🤖 Modelos IA en N8N

**🎬 Vídeo (8.2 min):** https://www.loom.com/share/4c2c883b9fae4d2983b3efa9d5a7ab8b

---

En este video vemos cómo elegir y configurar el modelo de inteligencia artificial para tu agente en N8N.

Revisamos las opciones más recomendadas (como GPT-4.5 y O4 mini), la diferencia entre modelos generales y de razonamiento, y cómo ajustar parámetros como *reasoning effort*, *temperature* o *max tokens* según la tarea.

Te explicamos qué modelos funcionan mejor según el caso, cuándo vale la pena invertir más tokens y cómo evitar errores comunes al configurarlos.

## 🎙️ Transcripción

que van a tener con, o que la gente tiene con los agentes de inteligencia artificial, está relacionado al modelo de IA que se elige o se podría resolver con el modelo de IA que se elige en el agente. Bien, por eso vamos a entrar acá y vamos a detallar unos minutitos sobre esto. sobre cómo elegir un modelo de inteligencia artificial y cómo configurarlo en un agente de Neo4j. Vamos acá. Acá tenemos los diferentes modelos. Bien. No vamos a detallar todos estos, de cuándo sirve cada uno, porque hay algunos que ya directamente quedaron. viejos y no nos terminan sirviendo. Tenemos el normal, digámoslo así, que es el 4O. Este funciona bastante bien para tareas generales. Realmente, si requiere un razonamiento fuerte, no es no terminamos recomendándolo pero para algo general algunas tareas o lo más tranquilo puede puede agarrarlo bien. Por otro lado tenemos los modelos 4.1 y 4.5, nosotros acá siempre recomendamos el 4.5 preview el último que salga con la fecha o bien el 4.1 bien esto es muy leve la diferencia entre este modelo y este modelo si tenemos los modelos mini y los modelos nano, bien, los modelos nano yo particularmente no los recomiendo, no no no no no creo que vayan a ser útiles o más útiles que el 4 o los mini, son más baratos bien pero la realidad es que hay veces que lo barato termina teniendo caro y no terminas teniendo un buen resultado. Siempre cuando la cuestión de los costos de los token empieza a ser una, empieza a ser un interrogante, o empieza a tomar relevancia porque es que el volumen de llamados es altísimo, bueno ahí vamos a jugar un poco con economizar y ver de qué manera se puede optimizar y elegir un modelo más barato. Pero acá ahora no vamos a estar detallando de costos específicos porque la realidad es que no no termina afectando en un 9 a 10 casos, no termina afectando siempre que no elijamos los 2, 3 modelos carísimos que hay, pero más allá de eso los costos por más que sea casi gratis el 4.1 nano no te compensan, bien. el 4.1 mini o los modelos 4.2 4.1 mini o 4.0 mini bueno estos pueden servir para algunas cosas para algunas operaciones por ahí que no requieran mucho si o sea que requieran hasta menos que el 4o pero para muchas veces te termina zafando y termina funcionando bien y tiene un costo que no es considerable, es un costo extremadamente bajo. Repasando un poquito más vamos a bajar Quiero ver si me queda alguno en los 4.5, no, 4.5 estamos ahí, 4.0 y vamos a pasar con los de razonamiento, que los de razonamiento son mis preferidos. ¿Qué de razonamiento recomendamos? Los O3mini y no te termina compensando la diferencia entre lo 3 mini o lo 4 mini y en lo 3, por ejemplo. Entonces nosotros recomendamos mucho usar los modelos de razonamiento que son estos o 3 y o 4 en su versión mini. Bien, para la gente que esté comenzando con este esto y por ahí tal vez no sepa lo que son los modelos de razonamiento, son modelos que piensan paso por paso, lo cual hace que puedan llevar una lógica mucho mejor y puedan entender muchísimo más cómo llevar una conversación, sobre todo en los condicionales, en lo que es un árbol de decisiones, en un proceso de calificación, en un proceso de agenda, los modelos O3 y O4 son espectaculares. Bien, nosotros en casi todo estamos usando O4 mini, no notamos muchas diferencias con el O3 mini sinceramente, pero estamos ahí. Para este caso vamos a vamos a ir directamente con el O4 mini y vamos a empezar a ver acá todo lo que podemos todo lo que podemos configurar, acá te dan un montón de opciones pero muchas veces no se puede, por ejemplo si nosotros vamos a lo que es la temperature acá, reactivar realmente nosotros nos va a dejar N8n, configurarlo, le puedo poner si quiero 12 o 2, es el máximo, y me va a dejar, sin embargo en el movimiento hacer el llamado API no va a funcionar, ¿por qué? porque los modelos de razonamiento hoy, a día 19 de junio, esto puede cambiar, 2025, no te deja configurar la temperatura de los modelos de razonamiento. Así que en este caso lo vamos a dejar por acá. ¿Cuál es su mayor aliado, su mayor conflicto? configuración acá, reasoning effort acá. Acá nosotros podemos elegir si vamos a usar el O4 mini low, O4 mini medium o O4 mini high. Mientras acá hay una diferencia entre, bueno como lo explica acá, entre tokens o entre tokens. si gasto de tokens y razonamiento y velocidad lo mismo dentro de él por eso me gusta mucho los modelos de razonamiento porque dentro del modelo es como si tuviéramos tres modelos diferentes que funcionan más o menos de la misma forma pero que se puede hacer más rápido y se puede hacer más potente bien si nosotros vemos algo, una tarea que lleve razonamiento pero que no sea tan fuerte, podemos intentar con el low a ver si nos sirve un poquito la velocidad y si gastamos un poco menos de tokens pero el módulo de más potente o la configuración más potente sería ésta, ¿no? para las cosas, las tareas más pesadas que requieren más razonamiento, son cosas más sensibles y van a ver que hasta por ahí le puede tomar un minuto responder, pero muchas veces vale la pena. ¿Qué otras razones, qué otras cosas tenemos acá el maximum number of tokens y tenemos esto que es c***** puede responder o sea cuál es el máximo que nosotros queremos que responda lo mismo en los modelos de razonamiento no solemos configurar mucho más que esto pero para los otros modelos está Luego tenemos todo lo que es la Penalty, la Temperature y demás que esto, si nosotros vamos, por ejemplo, vamos a un, donde nos va a servir usar la Temperature, si usamos 4.1, vamos a 4.1 y yo busco, el temperature. Acá vamos a ver una gran diferencia entre una temperature de 1, una temperature de 2, una temperature de 0.7 que es el standard, seguimos con ¿Qué diferencias vamos a ver? Esto ustedes vean lo que es como un parámetro de creatividad. El modelo de IAP ve y tiene probabilidades o gestiona probabilidades. Mientras nosotros más temperatura le pongamos, más creatividad. más probable es que elijan la opción más rara, ¿sí? Entonces, eso lo tienen que tener en cuenta cuando tienen que hacer gestión de gestiones creativas como descripciones, como creación de textos. de tanto en otros modelos también puede ser para imágenes y además esto va muy bien y es un parámetro que sirve muchísimo. El resto, bueno tenemos el max retries que puede fallar que si falla que intente dos o tres veces, eso también funciona. pero realmente esas son las configuraciones más importantes, no quiero llenarlos con otro tipo de configuraciones porque el parito está acá, el mayor esfuerzo y el mayor retorno que van a tener en su esfuerzo está en esto que acabamos de contar. Vamos a dejarlo configurado de esta forma, vamos a darle al Reasoning Effort Medium y vamos a pasar ya con la siguiente parte.
